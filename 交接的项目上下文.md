# AI 电话评价 / 专家克隆 Agent｜项目交接上下文

> 交接时间：2026-09-01
>
> 交接状态：未完成的开发原型；可以运行和继续开发，但不能把当前 Looper Graph 或 Prompt 当成正式业务产物。
> 阅读优先级：继任者先读本文，再读 `AGENT_ONBOARDING.md`、`docs/AI电话评价Agent_主PRD.md` 和 `docs/AI电话评价Agent_MVP执行文档_v0.4.md`。`HANDOFF.md` 与 `AGENT_ONBOARDING.md` 已同步至 v0.53，但本文仍是最完整的交接入口。

## 1. 一句话说明这个项目

这个项目尝试把优秀猎头的访谈记录转化为一张“策略 + 路由条件 + 专家原话”的可审查 Graph，再从同一份获批 Graph 生成电话外呼模型的执行 Prompt，以及评价电话策略和话术的评价 Prompt。

当前第一目标不是发明一套理想猎头方法，而是先尽量忠实地“克隆”目标专家：还原他在不同候选人反应下怎么判断、怎么走下一步、会怎么说。等克隆可靠后，才考虑融合其他猎头策略做优化。

## 2. 产品方向和不可丢失的业务原则

### 2.1 只克隆目标专家本人

1. 只有目标专家本人亲口表达，或者目标专家明确认可的内容，才能成为该专家的策略或话术。
2. 访谈者、其他猎头和旁听者的发言只能作为上下文，不能直接算到目标专家头上。
3. 孤立的“对、嗯、是”不能脱离上下文单独成为策略。
4. 每条抽象策略都应回链到原访谈证据、说话人和时间位置。

### 2.2 Graph 的业务含义

- 节点：猎头在某个阶段采取的行动、策略或反应。
- 节点话术：该节点对应的一条或多条目标专家原话范例。原话非常重要，是模仿和评价的基准，不应被抽象描述替代。
- 边：候选人的反应、场景状态或路由条件，决定从一个策略节点进入哪个节点。
- 边条件允许模糊：真实电话很复杂，LLM 擅长根据上下文理解模糊语义。未知不等于否定，不能为了“字段完整”把条件写死或把不全信息废掉。
- 证据：留在后台和审查页面，用于解释节点、边和话术从哪里来；不应把整段证据无脑塞进外呼 Prompt。

### 2.3 最终产物

同一份获批 Graph 应能生成：

1. 电话执行 Prompt：供实际外呼大模型使用，包含策略节点、已选择话术、权威路由表、停止条件和 Graph 身份。
2. 策略评价 Prompt：评价实际通话是否遵循目标专家策略。
3. 话术评价 Prompt：评价话术是否接近目标专家表达。
4. manifest / 哈希：保证执行与评价使用的是同一 Graph、同一话术选择和同一路由表。

## 3. 项目如何来到当前状态

最初项目在公司电脑和公司账号/API 上运行。公司额度和设备出现问题后，项目通过压缩包转移到个人电脑。转移时缺少完整交接报告，因此后续工作依靠旧上下文、代码、PRD、执行文档和真实页面逐步对账。

重要迭代如下：

| 版本 | 主要工作 |
|---|---|
| v0.38 | 修复目标专家归属、分析运行身份、精确基线、任务隔离、Gate 状态和凭据安全。 |
| v0.39 | 明确当前目标是专家行为克隆；候选人反应是边，猎头行动是节点，目标专家原话属于节点。 |
| v0.40 | 增加从不可变来源创建干净重跑任务，避免同名 TXT 沿用旧 Gate 状态。 |
| v0.41 | 完整访谈相对精确基线做增量 Graph 学习；Graph 页面成为结构和证据审查中心。 |
| v0.42 | 明确边条件是路由核心；多出口空条件阻断，单出口空条件视为顺序衔接。 |
| v0.43 | 节点原话多选、编辑、保存版本、重置、删除和完整进入 Prompt。 |
| v0.44 | 边条件人工校准；模糊条件可用；执行/评价 Prompt 共享确定性权威路由表。 |
| v0.45 | 导入基线节点/边可通过候选 `modify` 编辑；规范 Graph JSON 可导出并重新导入。 |
| v0.46–v0.48 | 七阶段电话流程展示、相对父节点的局部分支、节点排斥、曲线边、条件聚焦，替换僵硬的全局左中右三栏。 |
| v0.49 | 节点拖动坐标自动保存；“重新初始化布局”清空人工坐标并恢复规范布局。 |
| v0.50 | 电话执行 Prompt 改为 LLM 执行说明层 + 后端确定性权威 Graph 附录；调用失败不静默降级。 |
| v0.51 | 修复精确基线 ID 被旧对象抢占和单个坏证据 ID 导致整项提案级联丢失；保留合法目标专家证据并记录 warning。 |
| v0.52 | 增加任务列表逐项删除和后端全链路级联清理；当时清空了旧 Looper 实验任务，只保留原始 Kiki 任务。之后又新建了 Looper 1/2/3 干净测试任务。 |
| v0.53 | 问题边冲突组增加来源、条件、全部目标和逐边定位；已解决“只报数量、找不到边”的一部分使用问题，但 Gate 跨任务一致性和 Graph 展示稳定性仍待专项审核。 |

## 4. 当前代码和运行方式

### 4.1 技术结构

- 后端：Python 3.13，标准库 `http.server`，主要逻辑集中在 `backend/app.py`。
- LLM 客户端：`backend/llm_client.py`，OpenAI 兼容接口。
- 前端：原生 HTML/CSS/JavaScript + Cytoscape.js。
- 数据：`backend/data/db.json` 单文件 JSON 数据库。
- 前端端口：`127.0.0.1:8897`。
- 后端端口：`127.0.0.1:8898`。

### 4.2 启动

```powershell
.\start.ps1
```

或手动：

```powershell
# 后端
cd backend
$env:AI_CALL_EVAL_API_KEY = '由公司重新提供的 Key'
python app.py

# 前端，另开终端
cd frontend
python -m http.server 8897 --bind 127.0.0.1
```

打开 `http://127.0.0.1:8897/`。

### 4.3 LLM 配置

当前非敏感配置：

```json
{
  "base_url": "https://token-hub.in.taou.com",
  "model": "gpt-5.6",
  "temperature": 0.7,
  "max_tokens": 65536,
  "timeout": 900,
  "thinking": {"type": "enabled"}
}
```

API Key 不在交接包中。必须通过 `AI_CALL_EVAL_API_KEY` 环境变量，或在当前后端进程中临时注入。公司接口可能要求连接公司内网。

### 4.4 Git 状态

- 分支：`main`
- 远端：`https://github.com/Tiiiim0725/ai-call-eval-agent.git`
- v0.53 收尾内容已按“先回归、再提交、再推送”的方式同步到远端；精确提交以 `git log -1` 和交接包清单为准。
- 当前仓库刻意保留运行数据库、清理前备份和回归脚本，便于继任者复现；不要先执行 `git reset --hard`、删除数据快照或覆盖工作区。
- API Key 不提交。拉取后仍须通过环境变量配置；非敏感模型配置可随仓库保留。

交接压缩包包含生成时的完整项目工作区和 Git 历史，继任者既可直接启动，也可从 GitHub 重新克隆。

## 5. 当前已经能做什么

1. 导入飞书会议 TXT，解析说话人、时间和发言。
2. 人工确认目标专家并通过 G1。
3. 使用完整访谈和完整基线调用 LLM，生成增量节点、边和触发条件。
4. 在 Graph 页面查看完整物化图（基线 + 本次增量）、既有基线和差异视图。
5. 编辑导入的既有节点和边；保存时生成候选 `modify`，不原地破坏基线。
6. 节点可保存多条专家原话，选择哪些原话进入节点话术，并保存人工校准版本。
7. 边条件可人工编辑确认，保留原始提炼条件和审计信息。
8. Graph 可导出为规范 JSON，再导入为下一份访谈的基线；布局标注和手工拖动坐标也可保存。
9. 已实现电话执行 Prompt、策略评价 Prompt、话术评价 Prompt、route table 和发布 manifest 的基本代码链路。
10. 已有多组 Python/Node 回归脚本。

以上“能做”只代表功能入口存在，不代表当前知识学习质量合格。

## 6. 用户实际使用顺序

当前产品是一份 TXT 对应一个任务。要连续学习多份访谈，目前只能滚动操作：

1. 导入 TXT1。
2. 确认目标专家和基线，过 G1。
3. 点击“基于当前基线学习整份访谈”。
4. 在 Graph 页面审查。
5. 点击“导出当前 Graph JSON”。
6. 新建 TXT2 任务，把上一轮 JSON 导入为新基线。
7. 重复以上步骤。

不要把新的访谈 TXT 当成“独立话术文档”导入；那个入口不是完整策略学习。

当前滚动方式已经暴露严重 ID 和语义问题，修复前不要把它当作正式工作流。

## 7. 四份 Looper 访谈的历史真实测试过程

以下任务已在 v0.52 收尾时从当前工作数据库级联清理，不再出现在网页中。完整清理前状态保存在 `backend/data/db.backup-before-task-cleanup-20260901_180054.json`，用于继任者复盘故障，不能当作当前运行库。

用户希望拿 Kiki 的图作为临时骨架，再依次学习四份 Looper 访谈。实际基线链如下：

| 阶段 | 任务 / 基线 | 图规模 / 本轮变化 | 拒绝项 |
|---|---|---:|---:|
| 初始 | `kiki_base_graph.json`，来自一个 Kiki candidate Graph | 34 节点 / 44 边 | 基线本身未经 Looper 确认 |
| Looper 1 | `task_25b5cbf1ac2f` | 6 节点变更 / 1 边 / 1 触发；导出后 38 节点 / 45 边 | 8 |
| Looper 2 | `task_a14cb9aa828e` | 6 节点变更 / 2 边 / 3 触发；导出后 39 节点 / 45 边 | 2 |
| Looper 3 | `task_457e68f43dc2` | 6 节点变更 / 1 边 / 2 触发；导出后 42 节点 / 46 边 | 11 |
| Looper 4 | `task_05fbef649394` | 5 节点变更 / 0 边 / 1 触发；当前仍为 42 节点 / 46 边 | 10 |

页面给人的感觉是：四份访谈只多了少量节点和两条边，像是模型什么都没学到。

## 8. 最新且最重要的故障诊断

### 8.1 结论

LLM 实际理解并提出了大量结构，主要损失发生在“LLM 结构化结果 → 服务端验证 → 落库”之间。同时，Kiki 被错误地当成 Looper 的精确知识基线，而不是未验证参考骨架。

四轮总漏斗：

| 对象 | LLM 原始提出 | 最终保存 | 保存率 |
|---|---:|---:|---:|
| 节点变更 | 31 | 23 | 74.2% |
| 边变更 | 22 | 4 | **18.2%** |
| 触发条件 | 12 | 7 | 58.3% |

共有 1,373 条访谈证据，最终拒绝31项模型提案。

第四份访谈并非模型没有输出边：模型提出了5条边，服务端把5条全部拒绝，因此页面显示0边。

### 8.2 根因一：导入基线 ID 与旧知识对象 ID 冲突

规范 JSON 导出时保留了类似 `ko_xxx` 的节点 ID。下一轮重新导入后，这个 ID 同时存在于：

1. 当前任务的合法 baseline node IDs；
2. 上一个任务的旧 `knowledge` 对象。

`validate_knowledge_linkage()` 先查全局旧知识对象，看到它属于旧 group，就返回 `cross_group_edge`；但该 ID 明明也属于当前精确基线，应优先按基线节点处理。

真实数据复现：同一条边在旧对象存在时返回 `cross_group_edge`，从内存副本移除同名旧对象后立即通过。

相关代码：`backend/app.py` 的 `validate_knowledge_linkage()`。

这造成约13项错误拒绝。

### 8.3 根因二：一条错误证据 ID 废掉整个知识对象

每个 `unknown_evidence_ref` 被拒对象实际上都有4～6条合法的 Looper 本人证据，只混入一个模型抄错的证据 ID。当前程序拒绝整个节点/边，而不是：

1. 保留合法目标专家证据；
2. 丢弃未知引用并记录 warning；
3. 标记为需要人工复核。

节点被拒后，引用其 `candidate_key` 的边和触发继续因 `invalid_endpoint_ref` 被拒，形成级联损失。

9个对象因一个坏证据引用被整项删除，又级联造成8项端点失败。

相关代码：`_expert_support_error()` 和 `llm_extract_strategy()` 中节点、边、触发的落库循环。

### 8.4 根因三：Kiki 参考骨架被当成 Looper 精确知识

策略提炼 Prompt 当前明确要求：

> 任务不是重新画一张独立流程图，而是对 EXISTING_BASELINE 做最小变更；基线已有且访谈没有改变的内容不要重复输出。

这适用于“同一个专家的上一版已确认 Graph”，不适用于“Kiki 图临时给 Looper 当骨架”。

Looper 没有提到某个 Kiki 节点，只能说明“尚未验证”，不等于 Looper 认可。当前系统却把沉默当作继续继承；candidate Graph 导出后又直接变成下一轮精确基线，进一步洗掉了未批准和跨专家来源差异。

### 8.5 根因四：超长输入和不可靠的 ID 抄写协议

四轮 LLM 输入约为 82,000～97,000 tokens；输出只有约5,400～7,000 tokens，输出上限65,536，因此不是输出预算耗尽。

输入包含完整基线和全部访谈上下文，其中目标专家发言占比约18%～40%。LLM 的分析摘要能正确提炼 Looper 策略，说明语义理解发生了；但要求模型反复抄写长随机 evidence/node/edge ID，极易出现单字符错误。

### 8.6 可恢复性判断

31项拒绝中：

- 13项是 baseline ID 被误判为旧 group 对象；
- 9项只有一个坏证据引用，但仍有4～6条合法目标专家证据；
- 8项端点失败依赖上述被误删节点；
- 只有1项 baseline trigger 引用目前看起来确实无效。

因此约30项具有恢复条件。不要把问题归咎为“模型没学到”。

### 8.7 2026-09-01 收尾修复结果

上述两个服务端根因已在本交接包的 v0.51 本地代码中修复：

1. 当前精确基线节点 ID 先于全局历史知识对象解析，边和触发器不再因同名旧对象被误判跨组。
2. LLM 候选证据改为逐条清洗：只要仍有当前来源的合法目标专家 SUPPORT，节点、边或触发器就会保留；未知、跨来源和错误说话人引用被移除或降为上下文并写入 `evidence_ref_warnings`。
3. 节点被保留后，依赖其 `candidate_key` 的边可以继续解析；真正不存在的端点仍拒绝。
4. 目标专家归属、孤立简短认可、G3、approved-only 编译和发布的严格边界没有放宽。
5. 策略 Prompt/Schema 身份已升级，旧完成分析不会被相同输入静默复用。
6. 新增 `scripts/validate_strategy_candidate_salvage.py`；连同既有回归共18项通过。

这不会原地修复四轮已经生成的历史候选。它们仍是重要故障样本；要获得完整结果，必须从不可变 TXT 创建干净任务重新学习。

### 8.8 v0.53 问题边定位修复与剩余风险

最新真实操作暴露了一个容易误解但后端判定正确的场景：两条边已经分别保存为 `confirmed`，但它们来自同一个节点、条件文本完全相同，却指向两个不同目标。单边确认只说明“这段文字被人工保存”，不能证明整个来源节点已经具备唯一可执行路由，因此整图仍应阻断。

真实冲突组为：

- 来源：`礼貌结束`；
- 条件：`这个属于后续流程，不用管`；
- 目标一：`二次回拨在约定时间打`；
- 目标二：`试着用拿到的号码加微信`。

v0.53 前端已把原来只有总数的红色提示改为：显示来源、条件、全部目标，并为组内每条边提供“查看 → 目标”按钮。浏览器回归证明按钮能打开对应边编辑器，后端仍返回一个 `duplicate_branch_condition` 冲突组。系统没有替用户改写真实条件或批准 Graph。

这个问题只解决了一部分：隔离测试和一个真实冲突组通过，但还没有证明候选切换、刷新、继承基线边、多任务和前后端缓存状态始终一致。执行文档 W-02 未完成。

### 8.9 Graph 展示稳定性现状

Graph 已经有电话阶段布局、拓扑自动分层、原始坐标、网格、布局画像缓存、LLM 布局分类、人工方向标注、拖动坐标保存和重新初始化。但真实使用仍观察到：

1. 相同业务内容在不同任务、候选、布局画像或页面缓存状态下，初始长相可能不同；
2. 页面可能在电话流程布局不可用时回退拓扑布局，但用户不容易理解为什么变了；
3. 密集图仍有边交叉、标签/节点遮挡和初始缩放过小；
4. “算法坐标确定性”不等于“浏览器重复加载和完整交互稳定”；
5. 人工拖动坐标、重新初始化、Graph 内容哈希变化及布局画像过期之间仍需系统性复测。

因此交接结论必须写成：Graph 展示功能已经实现并经过局部验证，但稳定性存疑，尚未通过多任务、重复加载、缓存/降级和密集图的使用级验收。布局问题不得污染策略 Graph、路由表或 Prompt，但会显著影响人工审核效率。

## 9. 修复状态与继任者必须优先处理的问题

### 已完成 P0：停止主要知识丢失

1. **已修正 baseline ID 优先级**
   `validate_knowledge_linkage()` 已先按当前选中基线解析 endpoint，再查询全局旧知识对象。

2. **已实现证据引用容错且保留归属安全**
   - 将 evidence/context refs 分成合法与未知两组。
   - 至少保留一条合法目标专家 SUPPORT 证据时，允许对象进入候选。
   - 未知 ID 不静默吞掉，写入 `evidence_ref_warnings` 供 Graph 审核与审计复核。
   - 一条合法目标专家证据都没有时才拒绝。
   - 仍然禁止把其他说话人当作目标专家证据。

3. **已恢复可解析的依赖边和触发**
   合法节点不再因一个坏引用被整项删除，依赖 `candidate_key` 可以继续解析；真正无法解析的端点仍保留为拒绝项。

4. **使用短期局部别名，减少 LLM 抄 ID**
   Prompt 中把真实 ID 映射为 `N001/E001/S001/C001`，模型只返回短别名，服务端再映回真实 ID。不要让模型反复复制长随机 ID。

5. **验证漏斗仍需补齐**
   后端已经保留成功落库、警告和拒绝信息，Graph 页面也能显示部分模型判断与阻断原因；但尚未稳定、完整地并列展示“模型原始提案 / 成功落库 / 警告待复核 / 真正拒绝”。仍可能只看到最终持久化数量，从而误判为模型没学到。

6. **已加入真实故障回归**
   至少覆盖：
   - baseline 节点 ID 与旧 group 知识对象同名时仍能作为合法 endpoint；
   - 5条合法目标专家证据 + 1条未知证据时对象保留并告警；
   - 被保留节点的 candidate_key 能被后续边解析；
   - 没有任何合法 SUPPORT 证据时仍阻断。

7. **问题边冲突组可直接定位**
   多出口空条件、明确不确定未确认及同源同条件异目标仍由后端复算阻断；前端现在直接解释冲突组并可逐边打开右栏。单条 `confirmed` 不会错误覆盖整个冲突组。

### P1：增加“参考骨架”模式

导入 Graph 时明确选择：

- `exact_baseline`：同一目标专家、已确认，可以继承。
- `reference_scaffold`：其他专家或公共模板，只提供结构参考，默认未验证。

参考骨架节点/边至少需要这些目标专家验证状态：

- `confirmed_by_target`
- `modified_by_target`
- `added_by_target`
- `unverified_scaffold`
- `rejected_for_target`

未被 Looper 提到的 Kiki 节点必须保持 `unverified_scaffold`，可以灰色显示在 Graph 中，但不能编译进入 Looper 电话 Prompt。只有确认、修改和新增内容可以进入正式克隆产物。

### P1：原生支持同一 Graph 连续学习多份 TXT

当前“一任务一 TXT → 导出 JSON → 下一任务再导入”的方式丢失来源语义并触发 ID 冲突。更合理的产品是：

1. 建立一个专家克隆项目 / Graph identity；
2. 连续添加多份访谈 source；
3. 每次学习生成该 Graph 的新候选版本；
4. 保留每条知识来自哪一次访谈；
5. 不需要用户手动导出再导入。

### P1：修复后重新跑四份访谈

不要继续在当前 `kiki+l1/l2/l3` 链上堆正式知识。建议：

1. 保留当前 `db.json` 和四轮 JSON 作为故障样本；
2. 完成 P0 修复和参考骨架模式；
3. 从原始 Kiki 流程图以 `reference_scaffold` 重新依次跑 Looper 1–4；
4. 比较 LLM 原始提案与最终落库，边接受率不应再只有18%；
5. 人工核对每个继承 Kiki 节点是否被 Looper 证据确认；
6. 通过后再生成 Looper Prompt。

## 10. Prompt 生成仍未完善的问题

### 10.1 当前电话执行 Prompt

v0.50 已实现：完整物化 Graph 交给 LLM 生成执行说明层，后端再追加不可改写的权威附录，附录包含全量节点、已选择原话、触发/停止条件、权威路由表和哈希。LLM 调用失败会显式失败，不再伪装为成功。

但它仍缺少成熟电话平台的运行壳。与用户提供的“AI猎头外呼prompt_looper_决策收敛版”相比，目前至少缺：

1. 每轮动作/输出协议，例如 `stay/wait/end`；
2. 明确且单调推进的电话状态机；
3. 多路条件冲突时的全局优先级；
4. 播放中断、听不清、重复播放和恢复策略；
5. 候选人/JD/运行时字段契约；
6. 固定话术与可自然改写范例的区分；
7. FAQ、安全边界和少量冲突 few-shot；
8. 可执行回归案例。

建议把最终 Prompt 分层：

- 电话平台运行契约；
- 产品/安全 Guard；
- 专家克隆 Graph；
- 已批准回归案例。

生产 Prompt 参考文件已作为交接补充资料收入压缩包。

### 10.2 评价 Prompt 已知问题

1. 话术评价仍可能读取全局 approved `script_fragment`，而不是只读取当前物化 Graph 的话术选择。
2. policy guard / scoring 输入虽然被收集，但没有完整注入 Prompt。
3. 固定 60/40 权重头与 PRD 的“不得使用未批准权重”存在冲突。
4. 策略评价节点是否全量包含仍部分依赖编译模型；目前只有 route table 是确定性追加。

当前用户决定先处理电话执行 Prompt，因此这些问题尚未修复。

## 11. Graph 页面和人机协作状态

### 已实现

- 基线 + 增量完整候选图。
- 基线、候选和差异三种视图。
- 节点和边右栏审查。
- 节点原话多选、人工版本、重置和删除。
- 边条件编辑确认、保留原始条件和证据时间。
- 导入基线节点/边通过候选 modify 修改。
- 七阶段展示标注、相对父节点分支布局、曲线边、局部聚焦。
- 拖动节点后自动保存坐标；重新初始化布局。
- Graph JSON 带布局导出和重新导入。
- 任务列表可逐项二次确认删除；后端同步清理该任务的证据、Graph、分析、基线、布局和发布链。

### 仍需改进

1. 原来的“03证据审核”业务价值很低。长期方向是把它变成后台工程，把结构和话术证据直接放到 Graph 审查页面。
2. Graph 页面应展示模型提案被拒绝的完整对象，并允许人工恢复/修正，而不是只显示错误代码。
3. 节点/边数量不是学习质量指标；应增加目标专家确认覆盖率、话术覆盖率、待验证骨架数量和路由可执行率。
4. 当前布局比早期版本功能更多，但不能宣称稳定；复杂边图仍有交叉、遮挡、缩放和重复加载差异。
5. 问题边 Gate 需要覆盖候选切换、刷新、继承基线边和至少三个独立任务，确认前端提示与后端批准结果始终一致。
6. Graph 布局需要按固定 Graph/布局哈希连续重复加载，并显式记录缓存命中、降级原因、人工坐标恢复和失败截图。

## 12. 部署和公司交接问题

当前程序只监听 localhost，必须在本机启动前后端，不能直接把一个 HTML 发给所有人就运行完整功能。原因是：

- 需要保存任务、Graph、版本和审计数据；
- 需要安全持有 API Key；
- 需要调用公司内网模型接口；
- 浏览器单 HTML 不应包含公司 Key，也不适合多人并发修改同一 JSON。

要让公司所有人通过链接使用，后续需要：

1. 部署后端和静态前端到公司可访问服务器或内部容器平台；
2. 使用服务端环境变量/密钥管理注入 API Key；
3. 增加登录、会话认证、任务权限和基本并发写保护；
4. 把 `db.json` 替换为 SQLite 或公司数据库；
5. 配置备份、日志和访问地址。

如果只是单人继任开发，解压交接包后运行 `start.ps1` 即可。

## 13. 数据、隐私和凭据

- `backend/data/db.json` 在交接包生成时包含 Kiki、Looper 1/2/3 四个 G2 实验任务，是继续测试后的运行快照，不是正式批准知识库。
- `backend/data/db.backup-before-task-cleanup-20260901_180054.json` 保存删除前8任务状态，仅用于故障复盘与恢复。
- `input-docs/`、`reference/` 和补充测试资料包含访谈文本和策略材料，仅用于公司内部项目交接。
- 交接包不包含 API Key。
- `.gitignore` 已忽略 `.env`、缓存和编辑器配置。
- 不要把 Key 写入 `llm_config.json`、README、截图或 Git commit。

## 14. 建议的继任开发顺序

1. 读本文。
2. 启动项目并记录当前 task、候选 Graph、基线、物化 Graph 与布局哈希。
3. 备份 `backend/data/db.json`。
4. 先完成执行文档 W-02：问题边 Gate 的多任务、刷新和候选切换稳定性回归。
5. 完成 W-03/W-04：Graph 布局/缓存/降级/人工坐标与密集图稳定性审计。
6. 运行 `validate_strategy_candidate_salvage.py`，确认 v0.51 收尾修复没有退化。
7. 实现参考骨架模式，并从原始 Kiki 流程图以 `reference_scaffold` 重新验证 Looper 1–4。
8. 人工确认 Looper 克隆 Graph 后，再完善电话执行 Prompt 运行壳。
9. 最后处理评价 Prompt 和多人部署。

## 15. 回归命令

先备份真实数据库，再执行：

```powershell
python scripts/validate_v038_alignment.py
python scripts/validate_graph_workflow.py
python scripts/validate_edge_conditions.py
python scripts/validate_edge_condition_routes.py
python scripts/validate_script_variants.py
python scripts/validate_baseline_edit_export.py
python scripts/validate_strategy_candidate_salvage.py
python scripts/validate_task_delete.py
python scripts/validate_graph_layout.py
python scripts/validate_frontend_h.py
python scripts/validate_h02_h05.py
node scripts/validate_call_flow_layout.js
node --check frontend/app.js
```

修改策略学习链路后必须新增并运行前述真实故障回归。不要只证明 API 返回200。

## 16. 目录重点

```text
ai-call-eval-agent/
├── 交接的项目上下文.md              本文，最新交接入口
├── README.md
├── HANDOFF.md                       v0.53 快速交接摘要
├── AGENT_ONBOARDING.md              v0.53 上岗与稳定性优先级说明
├── start.ps1
├── backend/
│   ├── app.py                       API、学习、验证、Graph、Prompt、发布主逻辑
│   ├── llm_client.py
│   └── data/
│       ├── db.json                  当前真实状态快照
│       └── llm_config.json          不含 Key
├── frontend/
│   ├── app.js
│   ├── index.html
│   └── styles.css
├── docs/
│   ├── AI电话评价Agent_主PRD.md      当前 v0.53-draft
│   ├── AI电话评价Agent_MVP执行文档_v0.4.md
│   └── progress.md 等历史资料
├── input-docs/
├── reference/
├── scripts/
└── runs/
```

交接包额外包含 `交接补充资料/测试物资` 和生产 Prompt 参考文件。

## 17. 最终状态判断

这个项目不是一无所有：Graph 审查、话术版本、边条件、布局、导入导出和 Prompt 骨架都已经搭起来，LLM 对四份 Looper 访谈也表现出了有效理解。

v0.51 已修复两类主要落库数据损失，v0.53 已补充问题边冲突组的可定位提示；但历史候选不会被原地改写，跨专家参考骨架仍被错误地当成精确知识，边 Gate 与 Graph 展示稳定性也没有完成系统验收。在干净重跑、参考骨架模式和 W 阶段稳定性验证完成前：

- 不批准当前 Looper Graph 作为正式克隆；
- 不把当前生成 Prompt 投入正式外呼；
- 不继续用 candidate JSON 滚动洗成“精确基线”；
- 不用增加输出预算或更换模型来掩盖验证器问题。

继任者最重要的工作不是继续堆 UI，而是先完成 W-02～W-04：验证边条件 Gate 在多任务、刷新和候选切换下是否一致，冻结 Graph 布局身份并复测密集图；随后再用干净任务验证 v0.51 的增量保留效果，并建立“参考骨架 ≠ 目标专家已确认知识”的明确边界。
