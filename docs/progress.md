# 第二位 Top 真人猎头克隆：进度记录

## 会话：2026-08-07｜系统级能力提炼新分支

### 新目标建立
- **状态：** 进行中
- 已完成：
  - 将研究范围从“电话内策略树”扩展为四个系统级主题。
  - 明确本轮先只重新取证 Looper，不提前混入 Kiki 结论。
  - 建立证据分层要求，避免再次把散点材料强连成完整流程。
  - 运行 session catchup，读取并保留旧分支的计划、发现与进度。
- 当前任务：
  - 完整回看 `looper学习1.txt` 与 `looper学习2.txt`。
  - 建立带时间戳的四主题证据集和首轮讨论稿。
- 新错误：
  - `git diff --stat` 失败，原因是项目目录不是 Git 仓库；不影响本地研究工作。

### 四主题首轮提炼
- **状态：** 完成
- 已完成：
  - 完整复核严格归属于 Looper 的 235 个发言段，并回到原始转录核对关键提问上下文。
  - 提炼 Looper 明示的“寻访—转换—推荐—面试—Offer”五行为骨架。
  - 将匹配拆分为商业门槛、候选人质量、职级推断、岗位关系、公司诉求关系、可落地性和动态验证。
  - 提炼二次通话与首次通话的差异，并发现“本次停止”与“未来再激活”需分层处理。
  - 提炼知识库三类来源、七类实体建议及公司/团队标签维度。
  - 明确排除发言人8在学习2 25:16提供的详细刷库流程，不归入Looper证据。
- 创建文件：
  - `系统级能力提炼/Looper系统级能力提炼讨论稿v0.1.md`
- 验证：
  - 文档共9个一级主题、33个二级主题；包含四类证据标签和四个目标主题。
- 下一步：
  - 优先与用户讨论匹配逻辑的对象拆分和维度设计。

## 会话：2026-08-05

### Looper 前置任务：制作话术学习策略文档
- **状态：** 完成
- 已完成：
  - 核验两份 Looper 学习记录与 Kiki v2 参考文档均存在。
  - 加载文档处理运行时，建立任务专用 QA 目录。
  - 解析出 235 个严格属于 `胡旭 Looper` 的发言段，并保留来源和时间戳。
  - 提取 Kiki v2 的完整块结构、section 参数、SHA-256、表格与段落内容。
  - 通过 Microsoft Word 成功导出 Kiki v2 为 5 页 PDF，并逐页检查参考版式。
  - 完成 Heading、图片、字段、脚注/尾注、内容控件审计。
  - 完成 Looper 内容草稿及 14 个时间戳证据点校验。
  - 生成 `looper话术学习策略文档v1.docx`，并完成首次结构与包级审计。
  - 经过 3 轮 Word/PDF 渲染检查，修复长表格行跨页问题。
  - 最终 7 页已逐页检查；18 项交付校验全部通过。
- 最终交付：
  - `looper话术学习策略文档v1.docx`
- 下一步：
  - 渲染/解析 Kiki v2 参考文档。
  - 完整解析两份 Looper 文本并建立仅含 Looper 发言的证据集。

### 阶段 1：资料盘点与策略结构提取
- **状态：** 进行中
- 已完成：
  - 接收并固化 Kiki 项目处理方法与第二位猎头的直接任务。
  - 明确当前不会生成最终评估 Prompt。
  - 建立持久化计划、发现和进度文件。
  - 盘点工作区文件；识别出大量 Kiki 资料和三个待归类文本文件：`全文.txt`、`C01.txt`、`C02.txt`。
  - 初查发现 `全文.txt` 为空；`C01.txt`、`C02.txt` 为 AI 猎头电话文本且存在中文乱码。
  - 通过原始字节核验确认两份样本为 UTF-8，并成功读取真实中文内容；乱码问题已解决。
  - 跨文件检索确认 `C01.txt`、`C02.txt` 的核心表达与 Kiki 完整交流记录同源，故排除为第二位猎头策略证据。
- 下一步：
  - 等待用户提供或指明第二位猎头的结构化策略文档。
  - 收到后先完整读取该文档，再判断是否需要补读完整交流记录。
- 创建/修改文件：
  - `task_plan.md`（新建）
  - `findings.md`（新建）
  - `progress.md`（新建）

## 验证记录
| 检查项 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 当前范围 | 不提前生成最终 Prompt | 已限制为资料取证与待确认结构 | 通过 |

## 错误日志
| 时间 | 错误 | 次数 | 处理 |
|---|---|---:|---|
| — | 暂无 | 0 | — |
| 2026-08-05 | 批量统计文档技能参考文件时发生 JavaScript 字符串语法错误 | 1 | 改用更简单的独立文件读取命令 |
| 2026-08-05 | Kiki v2 标准渲染失败：系统找不到 LibreOffice/soffice；随后哈希读取遇到文件占用 | 1 | 改为探测可用渲染器，并分开执行只读提取与哈希 |
| 2026-08-05 | Word COM 渲染脚本因 PowerShell 执行策略无法直接运行 | 1 | 使用显式 `-ExecutionPolicy Bypass -File` 调用受控脚本 |
| 2026-08-05 | `pdftoppm.cmd` 包装器无法找到内部路径 | 1 | 直接调用 Poppler 的 `pdftoppm.exe`，成功输出 5 页 PNG |
| 2026-08-12 | 两次追加新阶段时补丁上下文未匹配（中文输出乱码/上下文范围过大） | 1-2 | 显式按 UTF-8 重读文本，并拆分成稳定小补丁 |

## 会话：2026-08-12｜AI 电话评价 Agent 产品化前置理解
- **状态：** 完成
- 已完成：
  - 按规划技能检查上一会话状态，读取并保留既有 `task_plan.md`、`findings.md`、`progress.md`。
  - 从根目录确认已有 Kiki、Looper、策略节点图、评价 Prompt、系统级能力提炼和 QA/开发过程等多类资产。
  - 将本轮产品化目标和拟还原的五段主链路写入持久计划。
  - 完整核对 Kiki/Looper 原始交流记录、策略手册、节点—原话映射、直接话术、Draw.io 图谱及三类评价 Prompt。
  - 还原当前人工链：证据取证 → 策略/节点/边/话术提炼 → Graph/Overlay → 评价规则 → Prompt → case 验证。
  - 明确当前最成熟的增量原型是 `BASE_GRAPH + activated OVERLAY + GLOBAL_GUARD`，但仍缺自动 ingestion、证据级语义 diff、审批、版本发布、Prompt 编译与回归门禁。
  - 明确共享系统知识与各猎头独立策略模块必须分层保存，不能虚构统一 Top 猎头人格。
  - 形成供下一轮产品定义讨论使用的完整初步理解。
- 下一步：
  - 与用户讨论其已经想到的产品细节，并逐步明确输入契约、更新判据、人工审批边界、知识 schema、发布模型和回归门槛。
- 文件修改：
  - `task_plan.md`（新增阶段 E）
  - `findings.md`（记录本轮产品目标和初步目录发现）
  - `progress.md`（新增本次会话日志）
  - 本轮收口时仅更新三份规划记忆；未改动业务文档、Prompt、图表或源码。

## 会话：2026-08-12｜产品 PRD 共创启动
- **状态：** 进行中
- 已确认：
  - PRD 作为唯一需求真源并长期维护。
  - 执行文档在 PRD 形成获批基线后再生成，且只能派生 PRD 已收录内容。
  - 六项知识治理原则继续作为需求约束。
  - 后续重点讨论人工介入、复核信息、信息磨损、置信度、版本对比和跨专家体系吸收。
- 已完成：
  - 恢复上一阶段规划记忆并核对工作区。
  - 确认当前不存在本产品的正式 PRD，且目录不是 Git 仓库。
  - 建立阶段 F 任务与协作契约。
- 当前步骤：
  - 先确认 PRD 是只定义首个 MVP，还是同时定义完整产品愿景并圈定 MVP。

## 会话追加：2026-08-12｜PRD 结构确认
- 用户确认采用方案 A：一个主 PRD，覆盖完整愿景、MVP 和后续阶段。
- 新建 `AI电话评价Agent_主PRD.md` v0.1-draft；仅写入已确认目标、六项治理原则、文档协作规则和待讨论章节。
- 下一问：确定首个 MVP 的首要用户与核心使用场景。

## 会话追加：2026-08-12｜MVP 用户与下游边界确认
- 用户选择 B：首版供公司内部产品/运营人员使用，并可在 Top 猎头访谈结束后进入工作流。
- 明确本产品止于输出经过审核的新版评价规则；真实 AI 猎头电话评价属于 mentor 的下游评估 Agent/平台。
- 只读访问 EVALLAB v0.6 并核对运行评估、文档测试集、评估报告和评委 Agent 页面；未执行任何配置或测试操作。
- PRD 更新至 v0.2-draft，新增用户、核心场景、MVP 起终点、非目标和下游平台观察。
- 下一问：明确 MVP 的正式下游交付物形态。

## 会话追加：2026-08-12｜评价规则发布包确认
- 用户确认选择发布包方案 B；未来可能升级为直接发布方案 C。
- PRD 更新至 v0.3-draft，确认发布包最小组成，并将易误解的“适用猎头/策略体系”改称“策略配置与评估适用范围”。
- 下一步：解释并确认该适用范围的实际含义与字段边界。

## 会话追加：2026-08-12｜发布包范围字段确认
- 用户确认发布包拆分专家来源、策略编译配置、评估适用范围。
- 评估适用范围仅标记首次电话触达、二次电话触达或同时覆盖；非电话环节在产品级排除，不在发布包反复列举。
- PRD 更新至 v0.4-draft。
- 下一问：明确一次知识更新任务的首版输入材料。

## 会话追加：2026-08-12｜单入口与自动补全确认
- 用户确认 MVP 初始只输入一份交流记录文本；系统先读全文并自动填写会议、人物、角色和已出现的业务上下文，缺失时再人工补录。
- PRD 更新至 v0.5-draft，新增单入口、来源绑定、自动预填和渐进式补录要求。
- 下一问：确定首版需要支持的文本输入方式与文件格式。

## 会话追加：2026-08-12｜标准 TXT 输入确认
- 用户提供标准飞书会议转写 `looper学习2.txt`；完成只读结构核对和说话人统计。
- PRD 更新至 v0.6-draft，首版输入收敛为标准 `.txt`，并明确会议头、发言块、双时间戳格式、匿名标签保留和原文定位要求。
- 一次统计命令因 PowerShell 缺少右花括号失败；确认单点语法原因后修正命令并成功取得统计结果，文件未受影响。
- 下一问：确定说话人角色自动识别后的人工确认方式。

## 会话追加：2026-08-12｜Gate 1 说话人确认
- 用户选择强制整体确认方案 A；PRD 更新至 v0.7-draft。
- 明确该 Gate 位于策略抽取前，不能因模型高置信度跳过，并需保存人工修改审计记录。
- 下一问：确认同一说话人标签跨片段变化角色时的数据表达和复核方式。

## 会话追加：2026-08-12｜目标专家条件确认替代全量审核
- 用户确认撤销 v0.7 的全量说话人强制审核设计。
- PRD 更新至 v0.8-draft：只识别目标 Top 猎头，高置信度自动继续，低置信度、多候选或冲突时人工确认；其他发言仅作为上下文。
- 下一问：讨论目标专家发言如何分类，以及哪些分类必须人工复核。

## 会话追加：2026-08-12｜策略/话术知识层级校正
- 用户校正核心本体：策略是 Graph/fragment 的行为节点与条件边结构；话术是节点下的段落功能、句法和用词层。
- PRD 更新至 v0.9-draft，撤回扁平六类分类，改为知识层级、结构覆盖、适用范围、证据语境、审核状态等多轴标注。
- 新增跨专家不默认融合原则，并记录“不同自洽局部最优体系”作为待深入设计的治理问题。
- 下一步：先厘清通用/条件性策略与 Graph/fragment 的正交关系，再讨论假设回应与跨体系兼容。

## 会话追加：2026-08-12｜访谈规划与完整度反馈
- 用户确认三类泛化状态，并提出围绕同一专家反复补访的产品闭环。
- PRD 更新至 v0.10-draft：新增访谈信息收集结构、访谈后完整度/补访报告、复杂经验型多动作策略与自由裁量表达。
- 下一问：确认访谈前提纲生成是否与访谈后报告一起进入 MVP。

## 会话追加：2026-08-12｜信息完整度基准待 Mentor 确认
- 用户要求暂不拍板完整度算法，标记“闭环自洽 vs 必采信息结构”的核心分歧。
- PRD 更新至 v0.11-draft，新增 `MENTOR-Q-001` 五项可直接询问 Mentor 的问题。
- 下一步：等待 Mentor 反馈；其他 PRD 议题可继续讨论，不依赖本问题立即定案。

## 会话追加：2026-08-12｜未决议题盘点
- 用户确认 Mentor 只保留两问；已将其他完整度执行细节移回产品开放议题。
- PRD 更新至 v0.12-draft，新增 P0/P1/P2 未决议题地图和建议讨论顺序。
- 当前建议下一步从 `OPEN-P0-01`“评价对象与策略 Profile 绑定”开始，再进入最小知识 schema。

## 会话追加：2026-08-12｜Mentor 问题与缺口清单校正
- PRD 更新至 v0.13-draft：Mentor 区块明确只保留两问，其余完整度实现问题归入产品开放议题。
- 补充并保留描述性专家知识/规范性评价政策、Profile 绑定和证据型无结构变更等 P0 问题。
- 已完成一次全量缺口盘点；后续按优先级逐项讨论，不要求用户一次回答全部问题。
- 增加 OPEN-P0-11：内部敏感数据的最小权限、保留、脱敏和导出审计边界。

## 会话追加：2026-08-12｜评价时人工选择专家策略话术组
- 用户确认下游每次评价时人工选择采用哪位猎头克隆出的策略话术组，不建立某个公司 AI 与某位专家策略的永久绑定。
- 将“运行时选哪一组”与“跨专家策略能否融合”拆成两个问题：前者已解决，后者继续作为局部最优/兼容验证议题。
- 主 PRD 更新至 v0.14-draft：关闭原 `OPEN-P0-01`，清理相关 Profile 绑定表述，补充发布包 ID/名称/版本和评价任务版本追溯要求。
- 下一问：确认 MVP 是否只允许选择一套完整专家组，完全不提供跨专家拼装；融合是否统一后置。

## 会话追加：2026-08-12｜MVP 跨专家融合边界确认
- 用户确认 MVP 只允许整套选择已批准的专家策略话术组，不支持跨专家 fragment/overlay 临时拼装，也不实现融合判断。
- 未来若开放融合，组合结果必须先作为新组独立命名、审核、版本化并通过历史 case 回归，不能在评价运行时临时组合。
- 主 PRD 更新至 v0.15-draft；原跨专家融合议题从 P0 移至 P2，不再阻塞 MVP 执行基线。
- 下一步进入 `OPEN-P0-02`：明确专家策略话术组及其内部知识对象的最小 schema。

## 会话追加：2026-08-12｜同一专家的多策略话术组
- 用户确认同一专家默认只有一套策略话术组；人工明确识别出多套目标、前提或运行逻辑不同且各自自洽的打法后，允许拆成多套独立组。
- 系统可提出更新既有组或新建组的变更提案，但不得静默拆组；每套组独立选择、审核和版本化。
- 主 PRD 更新至 v0.16-draft，并在核心知识层级中补入“专家—策略话术组”的一对多关系。

## 会话追加：2026-08-13｜不可变基线与独立平台决定
- 用户确认已批准知识基线不可原地修改；新访谈提炼结果先作为候选知识/变更提案，人工批准后才生成新知识版本、策略话术组版本和评价规则发布包。
- Mentor 已决定本产品单开平台，不并入其 EVALLAB/评估平台。主产品负责知识更新、审核、版本管理、Prompt 编译和发布包生成；外部平台只通过人工/文件方式消费发布包。
- 主 PRD 更新至 v0.17-draft：补充 `CHG-*`/`VER-*` 版本治理、独立架构边界、人工交付契约，并清理 API/自动同步作为本产品路线或上线前提的表述。
- 回归执行不依赖 EVALLAB，具体由本平台执行还是人工外部运行后上传，列为后续开放问题。

## 会话追加：2026-08-13｜Schema 讨论启动：Graph、Fragment、Overlay 与融合
- 用户修正了此前“Looper=Fragment 集合”的潜在误读：fragment 更可能表示访谈和提炼尚未把复杂策略体系连接完整，而非专家本身没有完整链路。
- 当前讨论方向：主 Graph 与未连接 Fragment 同属一个持续完善中的专家策略话术组；完整度报告应识别缺口和待连接关系，而不是用 Graph/Fragment 形态评价专家优劣。
- Overlay 暂未被用户理解或确认；已解释为“基于某个已存在基线的、有作用范围的叠加变体层”，建议先从 MVP 正式结构类型中降级，待真实案例再定义。
- 用户提出先独立保存各专家组，再进行跨专家对比与融合候选组生成。策略融合方法后置研究；话术不做简单拼接，优先合并功能结构、保留来源明确的表达变体，并将 AI 综合表达单独标注。

## 会话追加：2026-08-13｜Overlay 纳入 MVP 的轻量支持
- 用户确认 Overlay 也先纳入 MVP，因为同一专家的后续访谈可能产生相对于既有基线的二次触达/条件性叠加内容。
- 主 PRD 更新至 v0.18-draft：Graph 表示当前已连接范围，Fragment 表示尚未充分连接的局部知识，Overlay 表示相对明确基线的轻量条件性叠加对象。
- 组内是否纳入 Graph/Fragment/Overlay 由知识更新、人工审核和版本发布决定；评价运行时不允许零件级选择。
- 首版只要求 Overlay 的基线引用、触发/适用范围、作用位置、操作、证据和审核状态；复杂优先级/冲突/回退/跨组组合列为后续问题。

## 会话追加：2026-08-13｜Overlay 轻量语义收紧（PRD v0.20-draft）
- 将 Overlay 明确为“相对已有明确基线、由后续访谈/新增证据提出的候选叠加”，而不是无基线的第三种抽取形态。
- 候选必须经过证据审阅、人工审核和版本发布才可进入组版本/Prompt；无明确基线时按 Fragment 或普通结构变更处理。
- 完整度报告不把 Overlay 与 Graph/Fragment 并列计数，而是单列基线变化候选及其证据、审核状态。
- 复杂 Overlay 运行语义（优先级、冲突、回退、嵌套、跨组组合）继续保留为待讨论项。
- 已补充 MVP 的实现边界：Overlay 可被提案、对比、审核和编译纳入新版本，但不在评价运行时动态解释或自动合成。
- 已明确 MVP 中 Overlay 的基线必须来自同一专家策略话术组；跨专家/跨组叠加继续后置。

## 会话追加：2026-08-18｜OPEN-P0-02 最小知识 schema 定义
- 用户确认该部分属于“只要定了不出错即可”的基础契约，授权直接由系统定义，不再逐字段共创。
- 主 PRD 更新至 `v0.21-draft`，新增 5.5 节，确定 `expert`、`strategy_script_group`、`strategy`、`graph`、`fragment`、`overlay`、`node`、`edge`、`trigger`、`candidate_state`、`script`、`guard`、`policy`、`evidence`、`linkage` 的对象边界、稳定 ID、审核状态、版本关系、归属/适用双轴作用域和最小关系。
- 稳定 ID 采用类型前缀 + UUID；名称可改但 ID 不变。审核状态统一为候选、待确认、已批准、已拒绝、仅存档、已废弃；候选/待确认/被拒绝/仅存档/已废弃对象不得进入 Prompt。
- 明确 Overlay 必须引用同一专家策略话术组内的已批准基线；无明确基线按 Fragment 或普通结构变更处理；跨专家融合不进入 MVP schema。
- `OPEN-P0-02` 已从待拍板议题转为已确认；下一步按顺序进入 `OPEN-P0-03` 证据与信息磨损。

## 会话追加：2026-08-18｜OPEN-P0-03 证据与信息磨损定义
- 用户继续授权按顺序推进，并接受基础契约由系统直接定义。
- 主 PRD 更新至 `v0.22-draft`，新增 5.6 节，确定原始 TXT 字节快照、来源哈希、幂等导入、source/utterance/span 三层证据定位、上下文引用、抽象回链和跨次访谈追加规则。
- 规范定位以字节偏移为准，字符偏移仅用于展示；摘要、清洗、翻译、AI 改写和人工修改都作为派生内容保存，不得替代原文。
- 冲突证据通过 conflict_set、有效期和 supersedes 并存，不能静默覆盖；可发布知识对象必须 100% 回链到原始 TXT 证据，否则阻断发布。
- `OPEN-P0-03` 已从待拍板议题转为已确认；下一步进入 `OPEN-P0-04` 人工确认点与审批责任。

## 会话追加：2026-08-18｜MVP v1 先跑通最小闭环
- 用户确认当前不追求一次性把所有治理细节做重，优先让第一版逻辑可运行，再根据真实 case 暴露的问题迭代。
- 主 PRD 更新至 `v0.23-draft`，新增 6.4 节，明确第一版闭环为：TXT 导入 → 原始快照/解析 → 目标专家确认 → 证据片段 → 候选策略/话术 → 人工批准 → 新版本 → Prompt/发布包导出。
- 第一版允许保守默认值和人工确认；复杂置信度校准、全量变更门槛、自动回归、复杂权限和动态 Overlay 引擎后置，但所有未实现判断必须显式标记，不能静默推断。
- 用户纠正执行规范：先完成并批准 PRD，再从 PRD 单向生成执行文档，最后按执行文档分步实现；任何临时 `.py` 原型不作为正式执行依据，先搁置。
- 主 PRD 更新至 `v0.24-draft`；下一步仍先处理 `OPEN-P0-04` 的最小人工确认点，暂不进入代码实现。

## 会话追加：2026-08-18｜OPEN-P0-04 最小人工确认点与审批责任
- 用户同意先以足够保守、可运行的第一版定义治理规则，后续再由真实 case 驱动优化。
- 主 PRD 更新至 `v0.25-draft`，新增 7.2 节：G1 目标专家确认、G2 证据归类确认、G3 知识变更审核、G4 高风险政策/Guard 确认、G5 发布确认。
- 普通知识变更必须经 G3；公司级 Guard、禁用规则、评分底线、敏感数据处理和强制动作还必须经 G4 的政策责任人显式批准。无责任人时阻断。
- 每个 Gate 记录角色、审核人、决定、时间、理由、证据引用、对象前后差异与后续状态；MVP 允许一人兼任多个角色，但不得省略 Gate 记录。
- `OPEN-P0-04` 已确认；下一步按顺序处理 `OPEN-P0-05`：变更提案与发布门槛。

## 会话追加：2026-08-18｜OPEN-P0-05 变更提案与发布门槛
- 用户确认采用第一版保守规则：一次明确、可定位的专家原话只足以生成候选提案，永不自动批准或发布。
- 主 PRD 更新至 `v0.26-draft`，在 4.5 节定义候选提案、知识版本、发布包三个递进状态；所有结构/语义变更均需 G3，高风险政策/Guard 还需 G4。
- `add/modify/reclassify/link/attach` 需要原始证据回链及对象/端点；`replace/deprecate/split/merge/unlink/detach` 还需受影响基线和旧/新差异；`override/overlay` 另需同组已批准基线与条件范围。未经证明旧内容失效，不能以新内容直接替换或废弃旧内容。
- 会影响编译结果、评价行为、适用范围或 Guard 的发布需最小人工回归：核对规则差异；有适用历史 case 时复跑至少一个。没有 case 时可记录 `regression_gap`，但必须由 G5 显式接受，不能伪称回归通过。
- `OPEN-P0-05` 已确认；下一步进入 `OPEN-P0-07` Prompt 编译契约。

## 会话追加：2026-08-18｜OPEN-P0-07 Prompt 编译契约
- 用户同意继续按“先快速完成可运行第一版 PRD 基线”的方式推进。
- 主 PRD 更新至 `v0.27-draft`，明确 Prompt 只从目标专家策略话术组版本中已批准、被明确纳入的知识和政策编译；候选、废弃对象、跨组对象和未版本化默认规则均不得进入。
- 编译产物必须有不可变 manifest 和 Prompt 哈希；相同输入与编译器版本应得到相同输出。人工不能直接改正式 Prompt，如需修改必须回写受控源对象或编译配置并重新走版本、审核和发布。
- 优先级固定为：适用范围/前置条件 → 已批准 Guard/政策 → 策略结构 → 绑定话术 → 评分政策。范围不符为 `not_applicable`，证据不足为 `insufficient_evidence`，不能因缺信息直接失败。
- 按稳定 ID 去重，话术变体不自动合并；超出 Prompt 预算即阻断，禁止静默压缩、截断或删改。
- `OPEN-P0-07` 已确认；下一步进入 `OPEN-P0-08` 发布包自描述与人工交付契约。

## 会话追加：2026-08-18｜OPEN-P0-08 发布包自描述与人工交付契约
- 主 PRD 更新至 `v0.28-draft`，发布包定义为不可变交付单元，必须包含 manifest、完整 Prompt、输入/输出契约、变更/回归摘要和交付核验卡。
- manifest 固定包 ID/版本、策略话术组与知识版本、编译/政策配置、适用范围、Prompt/manifest SHA-256、回归状态和审核引用；发布包默认不导出原始访谈、评价通话或完整敏感证据文本。
- MVP 支持导出发布包或人工复制 Prompt；两者都必须随附核验卡。接收内容的 Prompt SHA-256 一致才可记为 `integrity_verified`，没有接收方反馈只能记为 `sent_unverified`。
- 平台保存交付尝试、包版本与哈希、交付方式、交接双方、时间和核验状态；外部改写或哈希不匹配的副本是非受控派生物，不能冒充本产品已发布包。
- `OPEN-P0-08` 已确认；下一步进入 `OPEN-P0-09` 描述性知识与规范性政策边界。

## 会话追加：2026-08-18｜OPEN-P0-09 描述性知识与规范性政策边界
- 主 PRD 更新至 `v0.29-draft`，确认专家实际做法/话术是描述性知识，默认只在对应策略话术组内生效；“我会/我不会/必须”这类专家表述也不会自动升级为公司政策。
- 规范性政策/Guard 单独版本化，作用域分为 `group`、`team/product`、`company`；只有明确批准作用域的政策才可跨组复用。
- 从专家知识升级为共享政策必须有规范性主张、作用域、来源/证据、风险与例外、冲突检查、G3 和 G4 政策责任人批准；无责任人或作用域的候选只能停留在专家组/待确认状态。
- 适用的硬性政策/Guard 优先于描述性策略；作用域重叠或普通政策冲突未裁决时阻断发布，不按最新文本静默选择。
- `OPEN-P0-09` 已确认；下一步进入 `OPEN-P0-10` 证据型变更。

## 会话追加：2026-08-18｜OPEN-P0-10 证据型变更
- 主 PRD 更新至 `v0.30-draft`，将 `evidence_support`、`counter_evidence`、`contradiction`、`explicit_rejection`、`scope_change` 定义为独立证据型变更，不直接改写已批准对象。
- 纯支持证据只追加证据记录，不改变 Prompt/评分；反例、明确否定和作用域变化只有在影响适用性或评价语义时，才必须转成 `modify/scope_change/replace/deprecate` 等结构变更。
- 矛盾证据进入 `conflict_set`，双方并存，人工裁决前不能静默选边；影响发布对象时阻断新版本/发布。明确否定也不自动废弃旧规则，必须判断是旧规则错误、专家行为变化还是 case 例外。
- G2 负责证据归类与作用范围；改变可编译语义的变更再走 G3，规范性政策再走 G4；只有语义变化触发 P0-05 回归，纯证据追加保留审计但不触发回归。
- 已发布对象和 Prompt 不原地回写；证据追加使用新的证据版本/更新 ID，旧 Prompt、包哈希和审核记录保持不变。
- `OPEN-P0-10` 已确认；下一步进入 `OPEN-P0-11` 最小权限与敏感数据治理。

## 会话追加：2026-08-18｜OPEN-P0-11 最小权限与敏感数据治理
- 主 PRD 更新至 `v0.31-draft`，以角色 + 任务范围 + 数据级别定义 MVP 最小权限，不引入复杂 ABAC/DLP。
- 数据分为 `D1_delivery`（可交付发布包）、`D2_knowledge`（结构化知识/证据 span/审核记录）和 `D3_sensitive_source`（原始 TXT、实名、候选人/JD 与完整上下文）。发布包只含 D1，D3 默认脱敏且不导出。
- 产品/运营、知识审核、政策责任、发布责任和下游使用方分别按最小职责访问；同一人可兼任，但不因此扩大任务范围或取消 Gate 审计。
- D3 查看需用途记录，下载/导出默认禁止，例外需授权理由、范围、文件哈希和接收方审计；引用中的原文不得静默删除或覆盖。
- 所有敏感查看、导出、脱敏、权限、交付和删除/归档事件写入不可变 `access_audit`；误授权或哈希不匹配时停止分发并标记异常。
- `OPEN-P0-11` 已确认；P0 未决项已完成第一轮定义，P1 仍待按顺序讨论，下一步先进行 PRD 一致性复核与获批基线准备。

## 会话追加：2026-08-18｜P0 草案成熟度标记与一致性复核
- 主 PRD 更新至 `v0.32-draft`，新增统一 `【MVP草案】` 标记和 12.2 归因表：P0-02/03/04/05/07/08/09/10/11 的字段细节、阈值、人工流程和默认值均明确标为第一版暂定；候选不进 Prompt、版本不原地覆盖、证据可追溯、冲突不静默、Gate/哈希/敏感审计等列为硬约束。
- 复核发现并修正四处一致性问题：manifest 自引用哈希；非 TXT 政策来源的证据回链；G3“请求修改/请求补充”命名不一致；政策责任人必要查看 D3 的权限例外未写入数据级别表。
- 后续真实 case 应按 12.2 表归因到对应 P0 草案，优先修订 PRD，再更新执行文档；目前尚未进入执行基线或代码实现。

## 会话追加：2026-08-20｜独立平台前端工作台与视觉形象首版草案
- 用户提出产品需要与 Mentor/EvalLab 平行的独立网页，并要求能看到背后状态、参数和控制口；不为了接入下游平台而设计结构或接口。
- 只读参考了用户提供的 EvalLab 页面：其可借鉴的界面语言是左侧导航、中央工作区、卡片式配置、状态标签、运行日志、结果明细和独立模型配置页；其真实评估运行、候选人 Agent、反应类型、评委 Agent 等结构不直接复制到本产品。
- 主 PRD 更新至 `v0.33-draft`，新增 4.10、`OPEN-P0-12` 和 `OPEN-P1-09` 首版草案：工作台/任务/证据知识/变更版本/Prompt 发布/审计设置六类入口；来源、证据、知识 diff、Prompt/manifest、事件日志、交付核验等窗口；任务、证据、知识、变更、编译、交付控制口。
- 视觉方向暂定为“证据实验台 + 策略编译控制台”：深色高信息密度、蓝色主操作色、状态颜色与文字并用、节点/证据定位符号，不做拟人化 AI 头像和营销型光效。具体视觉稿、组件、色值、品牌名仍待确认。
- `OPEN-P0-12` 和 `OPEN-P1-09` 尚未锁定；先等用户确认第一版界面模块，再进入执行文档，不开始前端代码。

## 会话追加：2026-08-20｜OPEN-P0-12 首版前端显示内容确认
- 用户确认首版前端的具体显示内容、六类入口、核心窗口、参数内容、控制口及与 Mentor/EvalLab 的独立边界。
- 主 PRD 更新至 `v0.34-draft`；`OPEN-P0-12` 标记为“首版已确认·MVP草案”，后续真实 case 可按成熟度标记归因迭代。
- `OPEN-P1-09` 仍保留为“证据实验台 + 策略编译控制台”的视觉工作假设；品牌名、Logo、色值、组件和动效暂不锁定。
- 尚未生成执行文档或前端代码；下一步进行 P0/P1 全量一致性复核和 PRD 获批基线准备。

## 会话追加：2026-08-20｜OPEN-P1-09 首版视觉参考确认
- 用户确认以提供的三张 EvalLab 截图作为首版视觉参考：深色后台、左侧分组导航、蓝色主操作色、卡片化工作区、状态标签、矩阵/详情/日志窗口和高信息密度控制台布局。
- 主 PRD 更新至 `v0.35-draft`；`OPEN-P1-09` 标记为“首版参考已确认·MVP草案”。品牌名、Logo、最终色值、组件尺寸和动效后置细化，不再阻塞前端需求基线。
- 视觉参考仅约束布局语言和信息密度，不复制 EvalLab 的品牌、业务字段、下游运行结构或接口。

## 会话追加：2026-08-20｜视觉资产内嵌与 MVP 执行文档初稿
- 三张用户提供的视觉参考图已复制到 `assets/ui-reference/`，并以内嵌图片形式写入主 PRD；执行 agent 必须读取这些本地资产。
- 主 PRD 更新至 `v0.35-draft` 后，已依据已确认的 P0/P1 基线生成 `AI电话评价Agent_MVP执行文档_v0.2.md`，细化为 A–G 阶段、约 15–30 分钟粒度的勾选任务。
- 用户已批准进入执行文档阶段；初始执行位置为 v0.2 的 `A-01`，每步完成后必须回写并勾选，暂不开始超出清单范围的实现。

## 会话追加：2026-08-20｜执行 A-01 完成
- 已读取主 PRD v0.35、执行文档 v0.2 和三张本地视觉参考图。
- 已生成 `implementation-notes.md`，记录实现范围、硬边界、视觉结论和明确不做事项。
- 执行文档 v0.2 已勾选 `A-01` 并记录验证结果；当前执行位置推进到 `A-02`，尚未开始代码实现。

## 会话追加：2026-08-20｜执行 A-02 完成
- 已在正式项目目录建立 `mvp-app/`：`frontend/`、`backend/`、`data/`、`tests/`、`static/`，并生成 README、启动脚本和静态 HTML 空壳。
- 已确定前端端口 `8897`、后端预留端口 `8898`；使用 Python 标准库静态服务验证 `127.0.0.1:8897` 返回 HTTP 200。
- 未接入 Mentor/EvalLab，未实现业务逻辑；执行文档 v0.2 已勾选 `A-02`，当前执行位置推进到 `A-03`。


## 会话追加：2026-08-20｜执行 A-03 完成
- 已实现前端布局骨架：`index.html` 含六个独立视图面板（工作台/知识更新任务/证据与知识/变更与版本/Prompt与发布包/审计与设置），每个视图都有匹配其入口类型的骨架内容（面板标题+描述+主工作区+右侧详情栏+底部事件流）。
- `app.js` 更新为导航点击时切换 `view-pane` 的 `is-active` 类，同时更新页面标题和副标题；`styles.css` 追加 `.view-pane{display:none}.view-pane.is-active{display:block}` 和 `.placeholder-text` 样式。
- 验证：启动 Python 静态服务器 `127.0.0.1:8897`，首页返回 HTTP 200（11577 bytes），`styles.css`（4980 bytes）和 `app.js`（1117 bytes）均正常加载。
- 布局与三张参考图的信息密度和层级一致：深色底色、左侧分组导航、蓝色主操作色、卡片式工作区、状态标签同时显示文字和颜色、底部日志流。
- 未接入后端业务逻辑，未复制 EvalLab 品牌/字段/接口；执行文档 v0.2 已勾选 `A-03` 并记录验证结果；当前执行位置推进到 `B-01`。

## 会话追加：2026-08-20｜执行 B-01 至 B-04 完成
- 用户纠正信息源：`case C01.txt`、`caseC02.txt` 是 AI 猎头通话评价测试用例，不是知识来源。真正的输入文件是飞书会议记录格式的真人猎头访谈记录，放在 `输入用猎头访谈文档/` 文件夹，包含 `kiki的话术学习1.txt`、`looper学习1.txt`、`looper学习2.txt`。
- B-01：后端 `app.py` 实现 task/source/session/utterance 数据结构，JSON 文件存储，稳定 ID 用 uuid4 生成，重复导入返回 409 不覆盖旧快照。
- B-02：实现飞书会议记录格式解析——第一行日期+时长、关键词段（"关键词:"独占行，内容在下一行）、文字记录段中"说话人 MM:SS\n发言内容"的逐条解析。导入 `kiki的话术学习1.txt` 解析出 87 条发言、1 个会话、18 个关键词。
- B-03：每条发言自动生成候选证据，含 evidence_id、source/utterance/span、char_offset（展示用）、byte_offset（规范定位）、source_hash、evidence_kind。
- B-04：证据数据结构含 conflict_set 和 status 字段，支持冲突并存不静默覆盖；前端 `app.js` 接入后端 API，工作台显示真实任务数/证据数，任务页显示任务列表和可导入文件，证据页显示证据列表和详情。
- 修复：PowerShell `Set-Content -Encoding UTF8` 会加 BOM 导致 Python SyntaxError，改用 `[System.IO.File]::WriteAllBytes` 无 BOM 写入。
- 执行文档 v0.2 已勾选 B-01 至 B-04，当前执行位置推进到 C-01。

## 会话追加：2026-08-20｜执行 C-01 至 C-03 完成
- C-01：后端 `app.py` 新增知识对象最小 schema——`create_knowledge_object`（4 种类型：strategy_node/script_fragment/policy_guard/scoring_rule）、`list_knowledge`、`get_knowledge_object`、`new_version`（已批准对象不可原地修改，生成新 ID 并保留 supersedes）；`/api/knowledge` GET/POST 接口。
- C-02：`diff_against_baseline` 函数将候选对象与同 scope 同 type 的已批准基线比较，返回 diff 和影响范围摘要（related_baselines/has_conflict/evidence_count）；`/api/knowledge/<id>/diff` 接口。修复 `list_knowledge` 关键字参数 bug（diff 函数误传 scope= 参数）。
- C-03：`create_change_proposal` 支持 12 种变更类型（add/modify/reclassify/link/attach/replace/deprecate/split/merge/unlink/detach/override/overlay）；结构变更必须带基线，所有变更必须带证据回链，否则 400 阻断；`/api/changes` GET/POST 和 `/api/changes/<id>` 接口。
- 验证：知识对象创建 `ko_e268b6c20a4d`、变更提案创建 `chg_a4bcfb25709b`、无证据阻断 `missing_evidence`、结构变更无基线阻断 `missing_baseline`、diff 返回 evidence_count=1 证据回链。
- 修复路由 404 问题：插入 C 阶段 GET/POST 路由时 404 return 语句丢失缩进导致不在方法体内；旧 Python 进程占用端口 8898 需先 kill 再重启。
- 执行文档 v0.2 已勾选 C-01 至 C-03，当前执行位置推进到 D-01。

## 会话追加：2026-08-21｜执行 E-02 至 E-06 完成
- E-02：/api/llm-generate-strategy-prompt 接口实现。将已批准策略节点（ko_bd37f4ec96e3 询问是否方便接听电话、ko_4f4ff7340ff7 介绍客户岗位机会）作为输入，LLM 生成策略流程评价 Prompt（5412 字符），含节点清单、分支触发准确度、流程终止控制三个评价维度，以及逐节点命中状态输出格式。LLM 配置快照（base_url、model、temperature、max_tokens）记入 manifest。消耗 789 tokens。
- E-03：/api/llm-generate-script-prompt 接口实现。将已批准话术片段（ko_8280453544a7 partial_script）作为输入，LLM 生成话术评价 Prompt，含内容覆盖度、策略执行度、用词语法维度。消耗 970 tokens。
- E-04：/api/compile 接口实现。编译产物 cmp_c61ac9122120，含 manifest（compiler_version v0.1-mvp、输入对象 3 个、strategy/script/combined prompt SHA-256、LLM 配置快照、LLM 使用量）、prompt_size=5412/32000 未超预算。相同输入+相同 LLM 配置可复现。
- E-05：/api/release 接口实现。发布包 rel_8024c184f7a0 v1，含核验卡（5 步验证流程：核对包ID→复制Prompt→重算SHA-256→比对→标记integrity_verified）和 prompt_sha256。发布包不含原始访谈、候选人/JD 或 D3 内容。
- E-06：/api/delivery 接口实现。交付记录 dlv_28b0cb0a425c，哈希匹配标记 integrity_verified；错误哈希测试（WRONG_HASH）正确标记 sent_unverified。
- 全流水线测试通过：导入 kiki→D-02 提取证据(20条分类)→D-03 策略提取(8节点7边2触发)→D-04 话术映射(8映射3脚本)→G3 批准3个对象→E-01 编译输入(3批准17阻断)→E-02 策略Prompt→E-03 话术Prompt→E-04 编译(含manifest+hash)→E-05 发布包→E-06 交付记录。
- 执行文档 v0.3 已勾选 E-02 至 E-06，当前执行位置推进到 F-01。

## 会话追加：2026-08-21｜执行 F-01 至 F-04 完成
- F-01：工作台总览接入任务、候选/已批准知识、阻断 Gate、发布状态与异常提醒；指标卡、当前任务流、详情/控制和事件流可导航到对应窗口。
- F-02：任务与来源/证据窗口接入任务选择、文件导入、D3 只读来源元数据、解析保留检查、证据详情和 LLM 候选提炼控制。重复解析实测返回 `already_parsed`，不覆盖原快照。
- F-03：知识/变更窗口接入对象列表、候选/已批准状态、Gate 审批控制、证据回链和 `/knowledge/<id>/diff` 基线差异展示；实测 diff 返回基线数量、影响范围和证据数。
- F-04：Prompt/发布/审计窗口接入编译详情、策略/话术 Prompt 生成、approved-only 编译、发布包 JSON 导出、交付记录、审计列表和 LLM 配置（API Key 脱敏）。后端补充 LLM 配置修改的不可变 D3 审计回执，实测返回 `audit_id`、`immutable=true`。
- 验证：前端 `app.js` 35 KB、Node 语法检查通过；8897 返回 HTTP 200；8898 `/health`、任务/知识/Gate/Diff/编译详情/发布详情/交付/审计/LLM 配置接口可读；前端控制口未触发真实 LLM 生成或新发布。
- 浏览器技能发现当前无可用浏览器实例，未完成截图级视觉验收；已将该项记录为 F 阶段待补，不虚报为已完成。
- 执行文档 v0.3 已勾选 F-01 至 F-04，当前执行位置推进到 G-01。

## 会话追加：2026-08-21｜执行 G-01 至 G-03 完成
- G-01：新增只读 `validate_g01.py`，验证已有演示任务从 task/source/evidence 到 approved knowledge、G3、compile、release、integrity_verified delivery 和 immutable audit 的连续 ID/哈希链路，结果 PASS。
- G-01 暴露历史兼容问题：两个已批准对象的 `evidence_refs` 使用 `utterance_id`。不原地修改历史对象；后端新增 `normalize_evidence_refs`，新建对象写入前将可解析的发言引用转成 `evidence_id`；验收器对历史对象保留间接回链计数（直接 1 条、历史发言回链 4 条）。
- G-02：新增 `validate_g02.py`，验证重复导入 409、重复解析不覆盖 409、未知来源 404、无证据变更 400、approved-only 阻断 17 个候选、已有哈希不匹配降级 `sent_unverified`、D3 快照/发言正文默认脱敏、immutable 审计，结果 PASS。
- G-02 安全修复：`/api/source/<id>/snapshot` 只返回元数据和 `snapshot_available`；`/api/utterances` 默认移除正文。解析/LLM 服务内部仍读取本地不可变快照。冲突集当前为 0；超预算和真实 LLM 失败重试未触发，留作下一轮回归。
- G-03：执行文档 v0.3、主 PRD 更新至 v0.37-draft（新增 12.3 集成验收发现）并完成本进度回写。当前 G 阶段完成；仅剩浏览器实例可用后的截图级视觉验收待补。

## 会话追加：2026-08-21｜新增 G 后方向纠正轮
- 复核结论：主 PRD v0.37 的产品目标、LLM 四个介入点、策略流程/话术双 Prompt、独立平台边界和人工 Gate 方向没有错误，不做方向性改写。
- 发现问题属于实现落差：目标专家 G1 未成为 D-02 前置阻断；Graph/Fragment/Overlay 及 Edge/Trigger 没有完整持久化；策略 Prompt 生成目前主要输入节点而非已批准 Graph；G1/G2/G5 尚未形成真正状态阻断；Graph/Script diff 仍偏文本对象 diff。
- 执行文档新增 `阶段 H｜方向纠正与 C/D/E 重基线`，当前执行位置切换为 H-01。H-01 先产出方向差异清单，H-02～H-05 分别修正 schema、G1/G2、Graph 编译和 Gate/Diff，H-06 重跑 G-01/G-02 并回写。
- 本轮原则：保留 A/B 的来源与证据基础设施，不为迁就现有扁平实现而修改 PRD；先暂停非必要 UI/LLM 功能扩展，完成 C/D/E 语义重基线。

## 会话追加：2026-08-21｜H-01 至 H-06 纠正轮完成
- H-01：`direction-diff_2026-08-21.md` 锁定方向差异；确认 PRD v0.37 无需方向性修改。
- H-02：知识对象扩展为 expert、strategy_script_group、graph/fragment/overlay、strategy_node/edge/trigger、script/script_fragment；新增关系和 evidence ID 校验，历史对象保持只读兼容。
- H-03：G1 必须确认 target_expert；G2 必须提交并批准 evidence refs；D-02/D-03/D-04 在 G1 前返回 `gate_required`；G3 仅能批准已通过 G2 证据的候选对象。
- H-04：策略 Prompt 只读取已批准 Graph 及其节点、Edge、Trigger、stop_conditions；缺 Graph/Edge/Trigger 返回 `insufficient_evidence`；编译遇到真实 LLM 错误不再用伪造 Prompt 静默通过。
- H-05：补充 Graph/Script 结构 diff；任务 Gate 按 G1→G2→G3→G4（适用时）→G5 推进；发布包校验 G5 和 Prompt 哈希，缺 Gate/哈希不发布。
- H-06：`validate_h02_h05.py` 隔离回归 PASS；`validate_g01.py`、`validate_g02.py` 继续 PASS。真实 LLM 探针返回 `429 DAILY_LIMIT_EXCEEDED`，浏览器实例仍不可用，均已明确记录为环境缺口。

## 会话追加：2026-08-21｜H 后前端契约补齐
- 发现旧前端存在编码损坏和静态按钮，未暴露 H 阶段新增的 Gate/Graph 契约；已重建 `index.html`、`styles.css`、`app.js`，去除乱码并接通真实后端状态。
- 页面现可执行：任务切换与 TXT 导入、G1 目标专家确认、LLM 证据/Graph/话术候选提炼、证据接受/改类/待确认/冲突与 G2、Graph/Edge/Trigger/Script 对象审核与结构 Diff、双 Prompt 编译、G4/G5、发布交付和 LLM 配置。
- 后端新增 `/api/evidence/review`，并修复 `/api/sources` 暴露 D3 原始快照的问题；列表接口现在只返回 `snapshot_available` 和 `snapshot_redacted`。
- `validate_frontend_h.py` PASS：前端/后端均为 HTTP 200、CORS 可用、8 个关键控制口存在、模型为 `gpt-5.6`、来源快照不出接口。H-02～H-05、G-01、G-02 回归继续 PASS。
- 浏览器控制环境仍无可用实例，未做截图级视觉验收；服务已启动，用户可在 `http://127.0.0.1:8897/` 直接验收。
- 追加修正：新增“选择本地 TXT”入口和 `/api/import-content`，浏览器读取用户本机 TXT 后由服务端执行 UTF-8 哈希去重、不可变快照保存和解析；隔离测试验证首次导入成功、同内容重复导入返回 `duplicate`。
## 会话追加：2026-08-25｜PRD v0.38 基线拉齐与 J 阶段完成

- 执行文档升级为 v0.4，并以转移后的完整项目为当前代码基线；历史 A–I 记录保留，新增 J-01～J-07。
- 目标专家限制从 Prompt 提升为服务端写入/G3 双层校验；SUPPORT 与 CONTEXT 分离，明确认可保存双证据，孤立短肯定不得成策略。
- 分析运行指纹补齐专家确认版本、基线 ID/版本/哈希、实际证据/上下文、请求/生效参数、模型、Prompt 与 Schema；重分析不覆盖已批准证据。
- 修复基线首项回退和话术导入原地修改 baseline；编译/发布按 task/group/baseline 隔离，任务切换清理派生状态。
- G2/G3/G4/G5 改为对象态与流程态分离，错误契约不再统一 500；前端未满足 Gate/Key 时禁止编译和发布。
- 公司旧 API Key 已从项目配置删除；长期 Key 仅从 `AI_CALL_EVAL_API_KEY` 读取，配置接口不返回 Key 片段；CORS 限制为允许的本地 Origin。
- `validate_v038_alignment.py` 5/5 PASS；H-02～H-05、G-01、G-02、前后端契约、Python/Node 语法和浏览器任务切换/发布/设置页检查通过。
- 外部遗留：等待可用 Key 后做真实 GLM 质量回归；I-07 多样例截图级 Graph 验收保持未完成。

## 会话追加：2026-08-25｜同一来源干净重跑入口

- 复现同内容改名上传仍命中 `task_81e793235e84` 且保持 G3；确认根因是内容去重与任务运行复用缺少显式分支，而非用户操作错误。
- PRD 升级至 v0.40-draft，新增 `IN-011`：普通重复上传仍去重，显式重跑创建独立 G0 任务且不继承旧审核和派生状态。
- 后端新增 `POST /api/tasks/rerun`；前端任务页新增“从当前来源新建重跑任务”，任务下拉与列表显示“（重跑）”。
- 重跑任务复制不可变 Graph 基线为新 ID，保持 Graph 内容哈希一致，供新 G1 重新选择；旧任务和旧基线不修改。
- 新增 `scripts/validate_task_rerun.py`，并通过 Python/Node 语法、v0.38 五项回归和前端契约验证。
- 真实页面已从旧 `looper学习3.txt` 创建 `task_ffc50e22c123`（G0），来源哈希仍为 `0235df3de13e...`，已复制 `流程图looper` 基线且哈希仍为 `cac96098a92f...`。

## 会话追加：2026-08-26｜精确基线增量 Graph 与 Graph 中心审核

- 主 PRD 更新至 `v0.41-draft`：正式学习明确为“精确完整基线 + 完整新访谈”的增量变化提案；每个节点/边/触发器携带 `change_type`、准确 `baseline_refs`、结构证据和目标专家话术证据，服务端拒绝虚构引用。
- 后端正式 Graph 提炼不再逐发言或逐话术截断，当前质量档为 GLM-5.2、`max_tokens=65536`、`timeout=900s`、思考模式开启；连接测试改为短确定性请求，模型空 content 明确报 `empty_content`，不再伪装为 JSON 解析失败。
- 候选存储保留增量变化，审核与 Prompt 编译统一物化为“未变基线 + 有效变化”的完整 Graph；最终组合 Prompt 字节预算由 32 KB 提高至 256 KB，仍坚持超限显式阻断、不静默裁剪。
- 默认导航移除逐条“03 证据审核”，Graph 成为主要人机审核面：同一画布切换候选完整图/现有基线/增量对比，点击变化对象查看结构依据、目标专家完整原话和回听时间；其他人发言只作折叠上下文。
- 新增整图批准/驳回接口。批准时后台逐项校验证据和基线引用、形成 G2/G3 审计，并从目标专家原话生成保留全文/说话人/时间戳的话术片段；真实 Looper 候选图只做查看验收，未替用户批准。
- Looper 真实页面验收：候选完整图显示 39 节点/55 边；修改节点能展示四条 Looper 本人完整证据和 `17:14/26:12/26:49/30:58` 回听定位；候选与基线不再左右拼接为两张独立图。
- 新增/更新 `validate_llm_transport.py`、`validate_v038_alignment.py`、`validate_graph_workflow.py`、`validate_frontend_h.py`；Python/Node 语法、干净重跑、H/G 回归和前后端契约全部通过。I-07 的 kiki/Looper/无基线多样例截图验收仍保留，不阻断当前 Looper 主 case。
- 追加真实高质量回归：第一次完整调用发现旧校验器错误拒绝两条连接未变化基线节点的边；修复后 schema 升至 `v0.39-2`，所有候选节点/边/触发器携带基线 ID/版本/哈希，基线端点通过精确哈希校验后可直接引用。有服务端拒绝项时 Graph 页面显式提示并阻断批准。
- 第二次 Looper 真实运行 `run_9926ca3ae77b` 完成：224 条证据，44,144 输入 token、35,062 输出 token（推理 31,862）、总计 79,206 token；生成最新候选 `ko_0c117d006c2f`，5 节点/4 边，节点/边/触发器拒绝项均为 0，完整物化结果为 35 节点/49 边。
- 高质量调用约 10 分钟；后端已改为并发 HTTP 服务，模型运行期间 `/api/health` 仍在 184 ms 返回。网页刷新后默认选中最新候选并锁定 `base_7d85e37c6e31`，Graph 画布正常渲染，未替用户点击批准或发布。

## 会话追加：2026-08-26｜Graph 边条件完整性

- 复现截图中的边条件显示为工程 ID；确认前端只是对空标签回退到 `edge.id`，Looper 原始 drawio 的对应边本身为 `value=""`，不是解析器漏掉独立 edgeLabel。
- 对不可变基线 `base_7d85e37c6e31` 的 45 条边完成拓扑分类：24 条显式条件、19 条唯一出口空边（确定性解释为“完成上一步后继续”）、2 条多分支空边（无法判断走向）。基线内容哈希 `cac96098a92f...` 保持不变。
- 两条真正缺失边均从“礼貌结束”出发，分别指向“二次回拨在约定时间打”和“试着用拿到的号码加微信”；系统未猜测或自动补写条件。
- PRD 升级至 v0.42-draft，执行文档完成 L-07；后端统一派生条件三态，并在整图批准、策略 Prompt 编译处阻断缺失分支；前端不再以边 ID 冒充条件，缺失分支标红、显示说明并禁用批准。
- 新增 `scripts/validate_edge_conditions.py`；Python/Node 语法、LLM 传输、v0.38、Graph 工作流、干净重跑、H/G 与前端全量回归通过。浏览器在真实 Looper 最新候选上确认“2 条分支边缺少条件”告警和禁用的批准按钮。

## 会话追加：2026-08-27｜节点原话选择、校准与版本管理

- 主 PRD 更新至 `v0.43-draft`，新增节点原话多选、原始证据不可变、人工校准快照、草稿物理删除、approved/编译引用保护及整图批准后才进入 Prompt 的产品契约。
- 后端复用 `script_fragment` 保存 `manual_corrected_verbatim`；候选节点用 `linkage.script_selections` 记录每条证据采用的版本，旧候选缺字段时默认全部初始版已选。新增节点工作区查询、保存版本、保存选择和删除草稿的最小接口，并校验任务、Graph、节点、专家和证据归属。
- Graph 右栏的目标专家原话已支持逐条勾选、版本切换、直接编辑、保存为新版本、重置初始版、永久删除草稿和节点级一次提交；未保存切换有原生确认，approved Graph 保持只读。初始访谈证据不会被覆盖或删除。
- Graph 物化、整图批准和话术 Prompt 统一读取当前选择；多条原话保持独立范例，未选版本不进入 approved-only 输入；删除当前草稿自动回退初始版。移除单条话术 200 字截断，仍受 256 KB 总预算显式约束。
- 新增 `scripts/validate_script_variants.py`；Python/Node 语法、v0.38、LLM transport、边条件、Graph、重跑、H/G、前端回归全部通过。
- 真实 Looper 最新候选五原话节点浏览器验收通过：显示“共 5 条 · 已选择 5 条”、5 张原话卡、5 个版本下拉和 5 个编辑器，初始版的永久删除按钮均禁用。未在真实候选上保存、删除、批准或发布。
- 继续采用 `db.json`，未引入数据库或缓存；仅在文件接近约 50 MB 或出现数万版本时再按实测评估 SQLite。

## 会话追加：2026-08-27｜边条件人工校准与无损路由 Prompt

- 主 PRD 更新至 `v0.44-draft`，明确模糊条件合法、未知不等于否定、字段不全不得丢弃或反转语义；人工只确认一段“路由条件”，其余身份、证据、时间、版本和审计由系统处理。
- 新增边条件查询/保存接口。候选边保存规范条件、原始条件、审核状态/人员/时间和可选不确定性；编辑继承基线边时创建精确 `strategy_edge modify`，不改不可变基线。任务、Graph、端点、基线版本/哈希和引用均由服务端校验。
- Graph 物化只将多出口空条件、明确不确定未确认和同源同条件不同目标列为问题边；单出口空条件保留为顺序衔接。人工未保存修改会锁定整图批准，问题边在画布标红；approved Graph 保持只读。
- 新增确定性权威路由表与电话执行 Prompt；执行 Prompt 和策略评价 Prompt 共享完全一致的路由表与哈希，明确按完整上下文理解语义线索、未知不得推导为否定、必要时自然澄清但不得发明新路线。编译、发布和交付保留执行 Prompt、路由表及独立 SHA-256，旧记录继续可读。
- 新增 `scripts/validate_edge_condition_routes.py`，并扩充 Graph/前端契约。Python/Node、v0.38、LLM transport、边条件、节点话术版本、Graph、重跑、H/G、前端回归全部通过。
- 真实 Looper `task_ffc50e22c123 / ko_0c117d006c2f` 只读验收确认：35 节点、49 条边，原始基线仍有 `PZGZyL7uIkHwY6AlBUR1-32/-33` 两条多出口空条件；二者可编辑且各自继续阻断批准/编译。边右栏单文本编辑器、保存确认、重置和 Prompt 页“生成电话执行 Prompt”入口均可见；未替用户填写、批准或发布。

## 会话追加：2026-08-28｜导入基线编辑与规范 Graph JSON

- 主 PRD 更新至 `v0.45-draft`：导入 Graph 始终作为不可变基线；编辑继承节点/边生成候选 modify；稀疏旧图缺失证据、话术和附加字段时保持为空，不伪造内容。
- 新增节点内容工作区和 Graph 右栏节点编辑器。候选节点可校准文本；编辑继承节点创建精确 `strategy_node modify`，基线版本和哈希不变。已批准 Graph 只读，未保存节点/边/原话修改统一阻断整图批准与无确认切换。
- 物化节点、边和触发器时改为“继承完整基线对象，再覆盖显式变更”，保留脚本、专家原话、证据/上下文引用、位置、条件审核信息和 metadata；既有手搓 Looper 图的空证据/空话术继续合法。
- 新增确定性规范 Graph JSON 导出；任务页的基线入口同时接受 draw.io/XML 和 JSON。导出文件可在另一任务重新导入为不可变公共基线，包含来源身份和内容哈希；公共证据引用只作可移植标识，不冒充本地已审核证据。
- 新增 `scripts/validate_baseline_edit_export.py`，覆盖稀疏兼容、基线节点/边修改、字段无损继承、哈希不变、导出/再导入、跨任务隔离和 approved 只读。全量 Python/Node、v0.38、Graph、Gate、编译发布、G/H、HTTP 与前端静态契约通过；真实 Looper 未被修改，浏览器视觉点击待用户本次验收。

## 会话追加：2026-08-28｜电话流程分区布局与展示标注

- 主 PRD 更新至 `v0.46-draft`，固定七阶段电话顺序与抗拒/中性/接受三类横向区域；布局只服务展示，不进入执行/评价 Prompt，也不改变 Graph、基线、路由表或编译身份。
- 后端新增独立 `graph_layout_profiles` 和布局查询/分析/保存接口。首次分析按物化 Graph 哈希缓存，严格校验节点/边 ID；遗漏保持 `unassigned/unknown`，人工覆盖与自动标注分开保存，approved Graph 仍可调整展示。
- Graph 页面新增默认“电话流程（七阶段）”模式、阶段带和区域标题；节点按固定阶段 Y 轴与入边方向 X 轴确定性排列，多入边冲突居中，正常前进边正交、回环边虚线。保留拓扑自动分层、原始坐标、网格和整图适配。
- 节点右栏可独立保存电话阶段/区域覆盖，边右栏可保存分支方向；这些动作不生成 `strategy_node/strategy_edge modify`。Graph JSON 导出/再导入可携带独立布局画像及哈希，缺少或无效布局仅警告。
- 新增 `validate_graph_layout.py` 与 `validate_call_flow_layout.js`，并扩充 Graph/前端契约；Python/Node、Graph、Gate、编译发布、HTTP 和确定性坐标回归全部 PASS。真实 Looper 仍为 35 节点/49 边，Graph 内容哈希保持 `a19a4193fb61...`。
- 公司 `token-hub.in.taou.com:443` 当前连接超时；两次真实 Looper 布局分析均保存为失败状态并安全回退旧拓扑分层，不阻断任何业务流程、不导出失败画像，也未替用户修改或批准真实 Graph。接口恢复后可在页面点击“重试布局分析”完成七阶段截图验收。
- 内网恢复后使用同一 Key 将模型切换为 `gpt-5.6`，短连接测试和现有深度思考参数均通过；真实 Looper 布局画像已生成并缓存为 ready。
- 首次真实布局截图暴露固定三个 X 中心造成的竖向拥挤。布局已改为三类区域内部按节点数动态横向扩宽、每节点独立 240px 槽位，阶段带和区域标题同步扩宽，初始视野居中首阶段；真实 35 节点坐标检查得到总宽 4240、横向跨度 3720、最小节点中心距 240、重复坐标 0。

## 会话追加：2026-09-01｜v0.53 问题边定位与稳定性交接

- 真实操作复现：两条从“礼貌结束”出发的边均已保存为 `confirmed`，但条件同为“这个属于后续流程，不用管”，分别指向“二次回拨在约定时间打”和“试着用拿到的号码加微信”。后端正确返回一个 `duplicate_branch_condition` 冲突组；重复点击确认不会消除同条件异目标冲突。
- 前端问题提示改为显示来源节点、当前条件、全部目标，并为冲突组内每条边提供直接定位按钮。浏览器回归确认按钮能打开相应边右栏；未替用户修改条件、批准 Graph 或生成 Prompt。
- 当前结论不是“问题彻底解决”：隔离测试和一个真实 case 通过，候选切换、刷新、继承基线边、多任务和前后端状态一致性仍需 W-02 回归。
- 用户继续反馈 Graph 显示不稳定。现有阶段布局、拓扑回退、布局画像、人工坐标和初始化能力均已实现，但密集图仍有边交叉/遮挡/缩放问题，不同缓存或候选状态下的初始长相也可能变化。执行文档新增 W-03/W-04，要求固定身份连续加载和截图级故障证据后再声称稳定。
- 主 PRD 与执行文档升级为 v0.53；README、HANDOFF、AGENT_ONBOARDING、项目交接上下文和后端说明同步更新成熟度边界。
- v0.53 收尾回归：15 个 Python 验证全部通过；`node --check frontend/app.js`、Node 布局契约和 Python compileall 通过，共 18 项；前后端 HTTP 契约通过，模型快照为 `gpt-5.6`，运行 `db.json` SHA-256 前后保持 `EF00F4C84919A4676897D10DD200D6938EDDDF09CCE02423AE59FF2B39A5FB51`。这只证明当次契约回归未退化，不代表 W-02～W-04 稳定性验收完成。
