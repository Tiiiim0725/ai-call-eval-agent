# AI 电话评价 Agent 交接包说明

版本：v0.53 开发交接基线
日期：2026-09-01

## 先说结论

本项目已经具备从访谈 TXT、目标专家与基线 Graph 出发，生成并人工审核增量 Graph、节点原话、路由条件以及执行/评价 Prompt 的完整开发骨架，可以继续运行和修改，但尚未达到稳定生产状态。

本轮已补充“问题边冲突组”定位：页面会列出来源节点、冲突条件、全部目标节点，并可逐边打开右栏。它解决了“只看到还有 1 个问题，却不知道是哪条边”的一部分使用问题。单条边显示 `confirmed` 不代表同一来源的全部路由已经可执行；同条件指向不同目标时仍应阻断。

该能力目前只完成隔离回归和一个真实冲突组的浏览器验收，跨任务、刷新、候选切换、继承基线边等稳定性尚未系统验证。

Graph 展示同样只可称为实验能力。阶段/拓扑布局、布局画像缓存、人工拖动坐标、重新分析和重新初始化已经实现，但密集图仍可能出现边交叉、遮挡、初始视野过小或重复加载结果不一致。布局不得改变策略、路由或 Prompt 真源，却会影响人工审核效率，须执行 W-03/W-04 专项审核。

## 继任者阅读顺序

1. `交接的项目上下文.md`：完整项目背景、真实过程、核心问题和下一步。
2. `HANDOFF.md`：启动、运行快照与风险速查。
3. `AGENT_ONBOARDING.md`：工程约束、数据语义和接手顺序。
4. `docs/AI电话评价Agent_主PRD.md`：v0.53 产品真源。
5. `docs/AI电话评价Agent_MVP执行文档_v0.4.md`：执行历史；当前从 W-02 开始。

## 当前必须优先处理

1. W-02：冻结任务、候选、Graph/基线身份，复测问题边保存后刷新、切换候选、继承边和多任务隔离。
2. W-03：对同一 Graph 连续重复加载，覆盖各布局模式、缓存命中/过期/失败降级、人工坐标与重新初始化。
3. W-04：保存截图、接口响应和身份哈希，形成“通过/不通过/已知限制”的稳定性结论。
4. 稳定性收敛后，再解决“参考骨架不等于目标专家精确基线”和增量学习漏斗可见性。

## 数据与安全

- 运行数据和故障复现快照随项目保留，均为开发测试资产，不代表已批准知识。
- API Key 不在仓库和交接包中；通过 `AI_CALL_EVAL_API_KEY` 环境变量配置。
- 当前模型和服务地址只是运行快照，精确值以 `backend/data/llm_config.json` 为准。
- 交接包中的测试结果只证明当次自动化与局部浏览器操作通过，不代表业务稳定性验收通过。

## 本次收尾验证

- 15 个 Python 验证全部通过。
- Python compileall、`node --check frontend/app.js`、Node 电话流程布局契约均通过；合计 18 项。
- 前端 8897、后端 8898 的 HTTP 契约通过，配置接口未返回 API Key。
- 测试前后运行 `db.json` SHA-256 保持一致，验证脚本没有改写当前任务数据。
- 上述结论不覆盖 W-02～W-04 的多任务、重复加载、缓存/降级和密集图人工可读性审核。

## GitHub

- 远端：`https://github.com/Tiiiim0725/ai-call-eval-agent.git`
- 分支：`main`
- 精确交接提交请在项目目录执行 `git log -1 --oneline`，并与压缩包根目录清单核对。
