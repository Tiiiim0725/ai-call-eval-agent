# Agent 上岗指南

> 本文写给接手本项目的下一个 AI agent（Codex 或同类）。
> 读完本文后再读 PRD 和执行文档，不要跳过。

> **2026-09-01 / v0.53 提醒**：项目功能很多，但成熟度仍是开发原型。路由问题边已能直接定位，Graph 布局也已有多轮实现；二者都不能据此宣称稳定。接手后先做执行文档 W-02～W-04 的重复加载与状态一致性验收，不要立即继续堆新功能。

## 1. 这个项目在干什么

公司有几位 Top 猎头（目前是 Kiki 和 Looper）。产品和运营人员跟这些猎头做访谈，
拿到了 TXT 格式的逐字记录。这些记录是散碎的、口语化的，不能直接当规则用。

**本项目的任务**：把访谈记录 → 提炼成结构化的策略流程图和话术 → 编译成"评价 Prompt"，
供下游的 AI 电话评价 Agent 使用，判断一通 AI 猎头电话打得策略对不对、话术像不像。

**产品形态**：一个独立的网页工作台（前端纯 HTML/JS，后端 Python 单文件 HTTPServer），
不和下游的 Mentor/EvalLab 平台合并，只输出 Prompt 文本和发布包。

**核心链路**：
```
导入 TXT → 解析发言 → 确认目标专家(G1)
→ LLM 证据分类(strategy/script/context/meta)
→ LLM 策略结构提炼(nodes/edges/triggers)
→ LLM 话术映射(direct_script/partial_script/...)
→ 人工 Gate 审核(G1→G2→G3→G4→G5)
→ 编译评价 Prompt → 发布包 → 人工交付核验
```

**关键点**：LLM 只做候选生成，所有知识状态变更和发布必须经过人工 Gate。


## 2. 文档体系与阅读顺序

| 顺序 | 文件 | 角色 | 什么时候读 |
|---|---|---|---|
| 1 | **本文** | 上岗指南，讲方法论和状态 | 第一个读 |
| 2 | `docs/AI电话评价Agent_主PRD.md` | 唯一需求真源，当前 v0.53 | 需要确认任何产品决策时查 |
| 3 | `docs/AI电话评价Agent_MVP执行文档_v0.4.md` | 执行 checklist（A-W 阶段） | 动手写代码前看当前进度 |
| 4 | `docs/progress.md` | 每次会话的工作记录 | 需要了解"之前干了什么"时查 |
| 5 | `docs/P0-P1决策草案_v0.21.md` | 未决项的候选方案 | 需要改 schema 或 Gate 规则时查 |
| 6 | `docs/direction-diff_2026-08-21.md` | H 轮方向纠正的差异清单 | 改 C/D/E 层时必看 |
| 7 | `docs/findings.md` | 原始资料研究（Kiki/Looper 证据） | 需要理解业务内容时查 |
| 8 | `docs/task_plan.md` | 更早期的任务计划（MVP 之前） | 了解历史脉络时查 |
| 9 | `HANDOFF.md` | 已知 bug 和文件结构 | 部署和修 bug 时看 |


## 3. 铁律（不可违反的工作规则）

这些规则散落在 PRD §0.1、执行文档 §1 和 P0-P1 草案 §0 中，这里集中列出。

### 3.1 文档优先级

1. **PRD 是唯一真源**。执行文档只能引用 PRD 中已批准的需求 ID。
   执行阶段发现新需求时，**先回写 PRD，再继续执行**——不许直接在代码里实现 PRD 没有的东西。
2. **决策草案不是真源**。`P0-P1决策草案` 是候选方案，未经 PRD 批准回写的，不能作为执行依据。
3. **版本治理以 PRD 版本号和 Git 历史共同为准**。当前目录已连接 GitHub `main`；修改前先看 `git status`，保留现有数据快照和用户改动，完成回归后再提交推送。

### 3.2 数据与知识治理

4. **原始 TXT 不可变**。导入后只做只读快照，解析后不可覆盖。
   重复导入返回 409，重复解析返回 `already_parsed`。
5. **已批准对象不可原地修改**。只能 `new_version` 创建新版本，标记 `supersedes`。
6. **候选/未批准/冲突未决对象不得进入 Prompt**。只有 approved 对象才进编译。
7. **所有抽象对象必须回链直接证据**。evidence_refs 只接受 evidence_id，
   不接受裸 utterance_id（历史对象保留兼容，新对象强制校验）。
8. **冲突不静默选边**。同一对象的矛盾版本用 `conflict_set` + `valid_from/valid_to` 并存，
   由审核决定废弃哪个。
9. **D3 原始正文默认脱敏**。API 不返回 TXT 原文，只返回 `snapshot_available=true`。
10. **正式 Prompt 不可手改**。前端不提供直接编辑已批准 Prompt 的输入框。
11. **发布必须经过全部 Gate**（G1→G2→G3→G4→G5），缺 Gate 返回明确错误，不静默发布。
12. **只有目标专家亲口的发言才能提炼为策略/话术**。
    其他人的发言标为 context，不提炼为节点。（这是 2026-08-24/25 的 expert-aware 补丁的核心。）

### 3.3 LLM 使用规则

13. **所有 LLM 产出均为候选**，不自动批准、不自动发布。
14. **LLM 不可用时不许伪造通过**。记录为 `llm_unavailable`，不得用规则拼接伪装成功。
15. **LLM 配置变更留不可变审计**。修改 base_url/model/api_key 时写 D3 审计回执。

### 3.4 工作流程规则

16. **执行文档必须与代码同时打开**。每个任务完成后填写"产出/验证/问题归因"，把 `□` 改为 `☑`。
17. **遇到 PRD 未覆盖的需求，暂停当前步骤**。先回写 PRD，再继续执行。
18. **不做 PRD 没要求的事**。不修无关 bug、不加无关功能。
19. **修复 bug 从根因修**，不做表面 patch。
20. **测试从改动的代码开始**，先窄后宽。代码库没有测试框架时不强行加。


## 4. 当前实现状态（2026-09-01 / v0.53）

当前已经形成 TXT → 目标专家 → 增量 Graph → 节点原话/边条件审核 → 执行与评价 Prompt 的完整代码骨架，并支持基线导入/编辑、Graph JSON、布局画像、人工坐标、实验任务删除和 LLM 包装执行 Prompt。这里的“完整”指功能面覆盖，不等于稳定或业务验收完成。

最新真实问题：同一来源节点的两条边即使都已人工 `confirmed`，若条件文字仍完全相同且目标不同，后端会继续阻断。这是正确规则，但旧页面只显示“1 项问题”，造成用户以为保存失败。v0.53 已列出冲突来源、条件、全部目标和逐边定位入口；目前只做了单个真实 case 和隔离回归，跨任务稳定性待验。

Graph 展示仍是实验能力：布局模式、缓存画像、候选内容哈希、人工坐标和重新分析会影响加载结果；密集图仍可能出现交叉、遮挡、初始视野过小或重复加载长相变化。策略/路由/Prompt 真源不受布局影响，但使用体验尚未稳定。

### 4.1 已完成

- **A 阶段**：项目目录、启动方式、视觉参考基线
- **B 阶段**：TXT 导入、不可变快照、发言解析、证据定位与回链
- **C 阶段**：知识对象 schema、候选/基线 diff、变更提案（12 种类型）
- **D 阶段**：LLM 配置模块、证据提炼、策略结构提炼、话术映射
- **E 阶段**：策略/话术 Prompt 生成、编译、发布包、交付核验
- **F 阶段**：前端六类入口全部接入后端（工作台/任务/证据/知识/发布/审计）
- **G 阶段**：G-01/G-02/G-03 集成验收（ID/哈希链路、阻断、脱敏）
- **H 阶段**：方向纠正轮（H-01~H-06），修复了 Gate 前置、Graph 持久化、编译输入
- **I 阶段**：Graph 可视化、drawio 基线导入、候选/基线/对照视图和人工匹配
- **J 阶段 / PRD v0.38 拉齐**（2026-08-25）：目标专家服务端归属校验、分析运行身份、精确基线、任务/组隔离、Gate 流程态、凭据安全和前端使用逻辑回归

### 4.2 本轮已收敛问题

| 问题 | 当前处理 |
|------|----------|
| 可配置模型在长输入、深度思考或结构化输出时超时/解析失败 | 当前快照为 `gpt-5.6`、高输出预算和长超时；必须保留显式错误与 analysis_run，不能靠截断或静默降级伪装成功 |
| 无节点、LLM 非 JSON 等统一返回 500 | 已拆分为可操作的 4xx/409/422/502 |
| 目标专家仅靠 Prompt 限制 | 已增加写入层和 G3 层 speaker/双证据校验 |
| 基线首项回退、话术导入修改 baseline | 已改为精确 ID/版本/哈希并生成独立候选 |
| 前端任务/编译/发布状态串线 | 已按 task 过滤并在任务切换时清空派生状态 |
| `_read_body` 编码/JSON 失败不明确 | 已兼容 UTF-8 BOM 并返回明确 400 |
| app.py 混合换行 | 已统一为 LF |
| Key 位于项目配置、通配 CORS | Key 已移出项目；CORS 限制为允许的本地 Origin |

### 4.3 环境与验收缺口

- API Key 已从项目移除；当前公司兼容接口与 gpt-5.6 曾连接成功，但凭据、模型路由和公司内网可用性均属于外部运行条件
- I-07 的 kiki/looper/无基线多样例截图级 Graph 验收仍未完成
- W-02 问题边 Gate 的多任务/刷新/候选切换稳定性未完成
- W-03/W-04 Graph 布局、缓存、降级、人工坐标和密集图可读性稳定性未完成
- 当前仅为 localhost 单机开发服务；开放到其他机器前必须增加会话认证和任务授权

### 4.4 数据状态

交接快照的 db.json 含 4 个 G2 实验任务：Kiki、Looper 1、Looper 2、Looper 3。v0.52 曾删除一批旧实验任务，随后用户重新建立干净测试任务；不要把历史清理记录误读成当前仍只剩 Kiki。精确任务 ID 见 `HANDOFF.md` 和 `交接的项目上下文.md`。


## 5. 代码结构

```
backend/app.py     — 后端主程序（3000+ 行），所有 API 路由 + 业务逻辑都在这一个文件
backend/llm_client.py — LLM 客户端（OpenAI 兼容格式，urllib 实现）
backend/data/db.json — 数据库（任务/来源/证据/知识/Gate/编译/发布/审计全部在一个 JSON）
backend/data/llm_config.json — LLM 配置
frontend/app.js     — 前端全部逻辑（670 行），无框架，纯 DOM 操作
frontend/index.html — 页面骨架
frontend/styles.css — 样式
```

后端端口 8898，前端端口 8897（或 8080）。前端 `app.js` 第 1 行硬编码了 API 地址：
`const API = 'http://127.0.0.1:8898/api';`


## 6. 怎么和用户继续合作

用户的协作风格和偏好：

1. **用户是产品/运营视角，不是开发者**。不要默认他懂代码细节，用业务语言解释。
2. **用户会直接说"有 bug"**，但不会精确定位。需要你自己排查、复现、修复。
3. **用户重视方法论**。先 PRD → 再执行文档 → 再代码的顺序不能乱，乱了他会纠正你。
4. **用户会做阶段性决策**。比如"是否接受 G1-G5 作为 MVP 人工 Gate"，
   你需要给出候选方案和推荐，他拍板后才回写 PRD。
5. **用户可能中途换电脑/换 agent**。保持文档和代码同步，让别人能接手。
6. **用户用中文**。所有文档、注释、commit message 用中文。


## 7. 下一步建议优先级

1. 冻结至少 3 个任务的 task/候选 Graph/基线/布局哈希，完成 W-02 问题边 Gate 重复回归。
2. 完成 W-03/W-04 Graph 展示稳定性审计：重复加载、缓存/过期/失败降级、人工坐标与密集图截图证据。
3. 完成 I-07 的 Kiki/Looper/无基线 Graph 多样例使用级验收。
4. 在上述稳定性问题收敛后，再继续优化增量学习质量与 Prompt 实际外呼测试。
5. 若要让其他机器或多人访问，先设计会话认证与任务授权，不直接放开监听地址或 CORS。


## 8. 启动方式

```powershell
# 方法 1：一键启动（推荐）
.\start.ps1

# 方法 2：手动分别启动
# 后端
cd backend; python app.py          # http://127.0.0.1:8898
# 前端（另开终端）
cd frontend; python -m http.server 8897 --bind 127.0.0.1  # http://127.0.0.1:8897
```

非敏感 LLM 配置可在“审计与设置”页面修改；API Key 必须通过 `AI_CALL_EVAL_API_KEY` 环境变量或当前后端进程临时注入，不得写入 `backend/data/llm_config.json`。
